# Spesa Intelligente — Contesto progetto per Claude Code

## Cos'è
App Flutter + Firebase di gestione budget familiare basata su "cash stuffing digitale": ogni entrata viene distribuita tra buste prima di essere spesa. Modello Free (analisi via regole, no AI) / Premium (AI generativa, scanner scontrino, previsioni).

## Regole obbligatorie — NON derogabili
- Prima di modificare codice, analizza il repository esistente. Non riscrivere da zero.
- Non eliminare funzionalità esistenti senza spiegare perché.
- Per ogni modifica indica: file, problema, soluzione, rischio, test.
- Procedi per piccoli blocchi, verifica la compilazione (`flutter analyze` + build) dopo ogni blocco.
- L'AI passa SEMPRE da Cloud Functions. Mai chiavi API nel client Flutter, mai hardcoded nel codice — solo `functions/.env`.
- Free non fa mai chiamate AI (costo zero). Premium/Trial sì, con limiti verificati **lato server**, non solo in Flutter.
- Le Firestore Rules isolano i dati per utente e per famiglia — mai lettura/scrittura di dati non propri.
- Se una scelta tecnica è incerta, fermati e proponi alternative invece di introdurre una soluzione fragile.
- Ambiente di sviluppo: Ubuntu. Emulatori Firebase locali per lo sviluppo, connessione reale solo in build release (già gestito in `main.dart` con `kDebugMode`).

## Design System
- Colori: verde `#16B98C` (primario), blu `#2563EB`, viola `#8B5CF6`, arancio `#F59E0B`, rosso `#EF4444`, grigio `#64748B`
- Font: Poppins via `google_fonts` (`AppTheme.light()` in `lib/theme/app_theme.dart` imposta `GoogleFonts.poppinsTextTheme()` come `textTheme` globale del `MaterialApp`, quindi ogni `Text` lo eredita automaticamente salvo `fontFamily` esplicito — verificato presente e coerente su tutto il progetto)
- Componenti: card arrotondate (elevation 0, `color: Color(0xFFF8FAFC)`, `borderRadius` 14–16), icone circolari colorate (`CircleAvatar` + `color.withOpacity(0.12–0.15)`), campi di testo filled/rounded (`filled: true, fillColor: Color(0xFFF8FAFC), border: OutlineInputBorder(borderRadius: circular(14)), borderSide: BorderSide.none`), AppBar bianca/elevation 0/testo e icone `AppColors.ink`, bottoni primari full-width arrotondati (`borderRadius` 14, testo bold, `foregroundColor: Colors.white`), progress bar sempre dentro `ClipRRect(borderRadius: circular(4))`
- Mantenere coerenza grafica in ogni nuova schermata — nessuna pagina fuori stile. Schermate allineate: Home, Buste, Spese, Statistiche, Premium, Scanner scontrino, Scanner scontrino famiglia (Block 11), Lista della spesa, AI Chat, Challenge/Sinking Funds, tutte le schermate Famiglia, Nuova entrata, Login (FASE 11), Onboarding demo + wizard di configurazione (vedi "Onboarding avanzato" più sotto).
- **Convenzione colore card AI (confermata):** sfondo `Color(0xFFF8FAFC)` (neutro) = contenuto informativo (es. Consiglio del giorno, Report mensile, Analisi abitudini, Analisi famiglia, Confronto scontrino/storico); sfondo `AppColors.primary.withOpacity(0.08)` (colorato) = proposta AI che richiede conferma esplicita dell'utente prima di essere applicata (es. distribuzione entrate suggerita, lista della spesa con budget suggerita). Applicare questa distinzione in modo coerente in ogni nuova card AI.
- **Verificato (controllo di stato) e corretto:** le card AI aggiunte dopo l'ultima passata di design seguivano già correttamente questa convenzione. `new_income_screen.dart` (dove vive la Distribuzione entrate) non era invece mai stato allineato al design system per il resto della schermata — ora sì: AppBar bianca/elevation 0/ink, campi filled/rounded, card "Assegnato/Da assegnare" e bottone finale coerenti col resto dell'app. Aggiunto ora alla lista "Schermate allineate" qui sopra.

### Icone custom (sistema centralizzato)

Preparato in vista della nuova Home (non ancora sviluppata — vedi mockup fornito dall'utente, non ancora incorporato nel layout): un sistema di icone proprietario, coerente in tutta l'app, al posto delle `Icons.*` di Material dove esiste un equivalente mappato.

- **Libreria:** `heroicons` (SVG, stile `outline`/`solid` coerente su tutta la libreria). **Non usare `phosphor_flutter`**: era la prima scelta ma è risultata incompatibile con l'SDK Flutter di questo progetto (la sua `PhosphorIconData extends IconData`, e `IconData` è diventata una classe `final` non più estendibile nelle versioni recenti di Flutter — la build web falliva con un errore di compilazione reale, non rilevato da `flutter analyze`). Verificare sempre con una build reale (`flutter build web --release`, l'unico target compilabile in questo ambiente) prima di considerare compatibile una nuova libreria di icone: l'analyzer da solo non basta per questo tipo di incompatibilità.
- **File:** `lib/theme/icon_palette.dart` (colori) + `lib/widgets/app_icons.dart` (enum `CategoryType`/`ActionType`/`NavType`, mapping icona+colore, widget `AppIcon`/`CategoryIcon`/`ActionIcon`/`NavIcon`/`IconBadge`). Uso: `CategoryIcon(type: CategoryType.casa)`, `ActionIcon(type: ActionType.scanner)`. Non chiamare mai `HeroIcons.*`/`Icons.*` direttamente per una categoria/azione già mappata — così cambiare un'icona per tutta l'app significa modificare una riga in `app_icons.dart`, non ogni schermata.
- **Palette icone:** definita in `icon_palette.dart` (`IconPalette`), **diversa da `AppColors`** in `app_theme.dart` — scelta esplicita dell'utente ("applica subito la nuova palette ovunque [nelle icone]"): la nuova palette (primary `#00A58E`, secondary `#008C8C`, accent `#8C8C8C`, estratta dalla futura Home) è già attiva su tutte le icone custom, mentre bottoni/AppBar/card del resto dell'app restano sul tema attuale (`AppColors`, verde `#16B98C` ecc.) finché non verrà sviluppata la nuova Home. È un disallineamento visivo temporaneo **voluto**, non un bug: quando arriverà la nuova Home, se serve allineare anche `AppColors` alla stessa palette, è una modifica centralizzata (i due file di palette), non schermata per schermata.
- **Colori categoria:** una base coerente di ~13 colori (`IconPalette.green/blue/orange/purple/amber/teal/indigo/pink/red/brown/cyan/emerald/gray`) riutilizzata sulle 26 categorie richieste (Casa=verde, Auto=blu, Spesa=arancione, Svago=viola, Scanner=viola, AI=giallo/arancio, Lista spesa=blu, Challenge=verde/acqua, Premium=viola, come da indicazioni dirette; le altre assegnate per coerenza tematica) — non un colore diverso per ognuna.
- **Cablato in tutte le schermate della richiesta originale** (senza toccare il layout della Home né di nessun'altra schermata, solo icone dentro layout esistenti): barra di navigazione inferiore (`NavIcon`, stato normale/selezionato), Home (azioni rapide, AppBar, drawer, alert budget, CTA), Buste (modifica/elimina/nuova), Spese (FAB), Statistiche (badge AI, pulsante analizza), Obiettivi/Challenge (FAB, icona tipo obiettivo risparmio/tetto di spesa, avviso superamento, template 52 settimane), Lista della spesa (pulsante AI, refresh, aggiungi, chiudi), AI Chat (invia, badge, report mensile con frecce su/giù e le 3 righe punto di forza/attenzione/consiglio), Premium (corona, checkmark funzionalità), Famiglia (invita/rimuovi membro/dashboard/nuova spesa/invito), Onboarding (le 4 icone pagina). **Lasciate volutamente Material** solo poche icone universali/decorative senza un equivalente di categoria sensato: `Icons.menu` (hamburger drawer), `Icons.account_balance_wallet` come marchio testata app in Home/Login (non un'icona di categoria). Le icone busta (`Envelope.icon`, emoji tipo `'🏠'`) **non sono state toccate**: sono dati utente esistenti per busta, non icone di categoria dell'interfaccia — cambiarle sarebbe una migrazione dati, non un lavoro di design system. Scanner scontrino (`scan_receipt_screen.dart`) **non toccato** per istruzione esplicita, anche se contiene ancora icone Material.
- **Impostazioni**: non esiste ancora come schermata dedicata nell'app — non c'è nulla da cablare lì finché non verrà creata.
- **Verificato:** `flutter analyze` pulito, build web reale completata con successo dopo ogni fase di cablaggio, e verifica visiva reale (screenshot via emulatore, due giri distinti) su nav bar, azioni rapide/AppBar/drawer Home, Buste (modifica/elimina), Spese (FAB), Statistiche (previsioni + badge AI, con e senza Premium/Trial attivo), Famiglia (dashboard, inviti), Challenge (template 52 settimane, icona tipo obiettivo), AI Chat (invio messaggio, report mensile) — icone nitide, colori corretti per categoria, nessun overflow in nessuna delle schermate toccate.

## Stato attuale — cosa è già costruito e funzionante

**Struttura:** `lib/models`, `lib/screens`, `lib/services`, `lib/theme`, `lib/widgets`, `functions/index.js`

**Modelli:** `Envelope`, `Expense`, `Income` (con allocazioni), `Challenge` (`ChallengeType.saving/spendingLimit`, con `envelopeId` opzionale per i Sinking Funds), `AppUser` (Premium/Trial + contatori uso), `Family`/`FamilyMember`/`FamilyInvite`, `FamilyExpense` (personale/condivisa/ripartita, senza doppio conteggio), `FamilyIncome`

**Schermate:** Onboarding (mini demo interattiva a 11 pagine con dati statici, mostrata una sola volta per dispositivo alla primissima apertura — anche prima del login — tramite `OnboardingService`/`shared_preferences`; sempre skippabile; per un account appena creato è seguita da un wizard di configurazione reale a 4 passaggi che scrive dati veri — vedi "Onboarding avanzato" più sotto), Login, Home (dashboard fedele al mockup ufficiale), Buste, Spese (elenco filtrato+raggruppato), Statistiche (grafico a ciambella `fl_chart`), Challenge (con sezione Sinking Funds), Premium (card con trial/stato abbonamento), AI Chat (chiama davvero `chatWithAssistant` passando un `spendingSummary` compatto costruito lato client — spese del mese corrente aggregate per categoria, entrate/spese totali del mese, quota mensile dei Sinking Funds attivi; niente transazioni singole né dati familiari, per tenere bassi i token e il costo per richiesta; il prompt vieta esplicitamente di fingere di eseguire azioni — vedi Problemi noti), Scanner scontrino (foto scontrino e/o foto prodotti, con abbinamento AI prodotto↔prezzo↔quantità quando entrambe le foto sono fornite; auto-creazione buste se l'utente non ne ha ancora, altrimenti selezione busta obbligatoria prima del salvataggio — mai un default silenzioso), Lista della spesa intelligente (suggerimenti basati sulla frequenza d'acquisto nello storico spese, nessuna chiamata AI quindi disponibile anche su Free; aggiunta manuale articoli), Famiglia (creazione, inviti via Cloud Function, membri, dashboard con filtro Famiglia/Membro, buste ed entrate familiari)

**Sicurezza:** Firestore Rules per utenti e famiglie; Cloud Functions con verifica auth + Premium/Trial server-side; chiavi solo in `functions/.env`; `main.dart` con emulatori solo in debug. Lo stato della lista della spesa (`shoppingListChecked`, `shoppingListManualItems`) è salvato sul documento utente esistente proprio per restare coperto dalle regole già in vigore, senza doverle allargare.

**AI Premium — consulente personale (in corso, piano approvato, si procede a blocchi):** vedi la roadmap sotto per l'elenco completo. Blocco 1 completato: `lib/services/budget_insights.dart` calcola avvisi di budget solo con regole/statistiche (busta esaurita, ≥85% di utilizzo, proiezione di fine mese al ritmo di spesa attuale) — zero chiamate AI, disponibili anche su Free, mostrati in una card su `home_screen.dart` (`_budgetAlertsCard`) solo quando ci sono avvisi da mostrare.

**Cloud Functions attive:** `chatWithAssistant`, `scanReceipt` (accetta `receiptImageBase64` e/o `productsImageBase64`, con `imageBase64` ancora supportato per compatibilità), `inviteFamilyMember`, `acceptFamilyInvite` — tutte con controllo auth + limiti trial. `verifyPlayPurchase` è scritta ma non operativa finché non è configurata su Play Console (vedi roadmap).

## Roadmap ancora da fare (in ordine)

0. **AI Premium — consulente personale di spesa intelligente**, in blocchi sequenziali (piano approvato):
   1. ~~Alert proattivi su buste/budget~~ — **fatto** (`budget_insights.dart`, Dart/Firestore, zero costo, anche Free).
   2. ~~Infrastruttura AI Premium condivisa~~ — **fatto**. `functions/index.js` ha ora `requireActiveAccess(userData)` (estratto da `chatWithAssistant`/`scanReceipt`, che ora lo riusano invece di duplicare il controllo), `requireAnalisiQuotaAvailable(userData, isPremium)` e `incrementAnalisiQuota(userRef)` sul contatore condiviso `analisiAvanzateUsate` (limite trial 10/mese, `TRIAL_MAX_ANALISI_AVANZATE` — va tenuto allineato a mano con `AppUser.trialMaxAnalisi` in Dart, stesso pattern già in uso per gli altri due contatori). Nuova Cloud Function `generateAiInsight`, per ora con un solo `kind` di prova ("test_echo"): i kind reali (`daily_tip`, `monthly_report`, `habit_analysis`, `family_analysis`) si aggiungono nei blocchi successivi. **Tutte le funzioni AI Premium proattive dei blocchi 3-9 useranno questo stesso contatore**, mai uno separato per funzionalità.
   3. ~~Distribuzione entrate assistita~~ — **fatto**. Cloud Function `suggestIncomeDistribution` (output strutturato con `responseSchema`, enum sui veri `envelopeId` come in `scanReceipt`, normalizzazione server-side per garantire che la somma non superi mai l'entrata dichiarata anche se il modello arrotonda male). Cablata in `new_income_screen.dart`: bottone "Consiglio AI per la distribuzione" (solo Premium/Trial) che costruisce un riepilogo compatto (buste con budget/saldo/% usata + quota mensile degli obiettivi di risparmio attivi, niente storico completo), mostra la proposta con motivazione, e la applica ai campi del form **solo** dopo tap su "Applica distribuzione" — il salvataggio resta comunque un'azione separata dell'utente. Copre solo l'ingresso da nuova entrata (punto 2); l'ingresso "chiedilo in chat" (punto 6) resta disponibile in prosa tramite `chatWithAssistant` già esistente, senza bottone azionabile — valutare in futuro se serve anche lì.
   4. ~~Contenuti AI proattivi con cache~~ — **fatto**. `generateAiInsight` gestisce ora anche `kind: "daily_tip"` e `kind: "monthly_report"`: aggrega i dati del mese corrente lato server (nuovo helper `buildPersonalMonthlySummary`, stessa logica già usata lato client in `ai_chat_screen.dart` ma eseguita con l'Admin SDK), chiama Gemini con `responseSchema` dedicato, scrive il risultato in cache su `users/{uid}` (`aiDailyTip: {text, dateKey}`, `aiMonthlyReport: {puntoDiForza, attenzione, consiglio, totalEntrate, totalSpeso, monthKey}`) e — se la cache per il giorno/mese corrente è già valida — la restituisce **senza richiamare Gemini né consumare la quota condivisa**, verificato negli emulatori. "Consiglio di oggi" ora è reale in `home_screen.dart` (`_DailyTipContent`, sostituisce il testo statico); "Il mio mese secondo l'AI" è una card espandibile in cima a `ai_chat_screen.dart` (`_MonthlyReportCard`), con i tre numeri (entrate/spese/risparmio) sempre calcolati in Dart/Firestore e solo le tre frasi narrate da Gemini.
   5. ~~Analisi abitudini e consigli di risparmio narrati~~ — **fatto**. `lib/services/habit_insights.dart` calcola in Dart, zero costo, la media mensile per categoria sugli ultimi 6 mesi completi (esclude il mese in corso) e le variazioni ≥20% dell'ultimo mese rispetto alla media precedente; se lo storico è insufficiente (meno di 2 mesi completi) restituisce una stringa vuota e l'AI non viene proprio chiamata. `generateAiInsight` gestisce `kind: "habit_analysis"` (riceve il riepilogo dal client, on-demand, nessuna cache — coerente col fatto che non è tra i contenuti proattivi del Blocco 4). Card "Analisi abitudini di spesa" in `analysis_screen.dart` (Statistiche), visibile solo Premium/Trial, richiede un tap esplicito — mai generata da sola all'apertura schermo.
   6. ~~Obiettivi intelligenti~~ — **fatto** (verifica + rifinitura, come previsto). Confermato che quota mensile e Sinking Funds erano già completi in `Challenge.monthlyQuota`/`challenge_screen.dart`, nessun nuovo lavoro lì. Aggiunto il pezzo mancante di "controllo automatico se il piano sta andando bene": nuovo campo opzionale `Challenge.createdAt` (retrocompatibile — le challenge create prima di questa modifica restano senza, gestito con null, mai un dato inventato) e getter `Challenge.isOnTrack` (Dart puro, zero costo, disponibile anche su Free) che confronta il progresso atteso (tempo trascorso/tempo totale) con quello reale, mostrato come badge "In linea col piano" / "Sei indietro rispetto al piano" sulle challenge di risparmio con scadenza. Il parsing di un obiettivo in linguaggio naturale resta scartato per ora (priorità bassa come da piano): l'inserimento resta tramite il form già esistente.
   7. ~~Scanner scontrino: confronto con la media storica~~ — **fatto**. Dopo lo scan, `scan_receipt_screen.dart` calcola (solo Dart/Firestore, zero AI) il totale di questo scontrino per ogni categoria già presente nello storico spese e lo confronta con la media storica di quella categoria, mostrandolo in una card informativa "Confronto con lo storico" sopra l'elenco prodotti — non blocca né modifica in alcun modo la conferma manuale già esistente (punto 11 rispettato alla lettera).
   8. ~~Insight famiglia~~ — **fatto**. `lib/services/family_insights.dart` calcola in Dart (zero costo) il confronto mese su mese delle spese familiari, totale e per membro (riusa `FamilyExpense.quotaFor` già esistente); se non c'è ancora nessuna spesa questo mese o il mese scorso, restituisce stringa vuota e l'AI non viene chiamata. `generateAiInsight` gestisce `kind: "family_analysis"` con un prompt che vieta esplicitamente giudizi sulle persone. Card "Analisi AI della famiglia" in `family_dashboard_screen.dart`, Premium/Trial, on-demand. I dati familiari restano assemblati lato client tramite le Firestore Rules già in vigore (`isMember()`): nessuna Cloud Function nuova legge `families/...` per conto di un altro utente.
   9. ~~Lista della spesa assistita da budget + chat più ricca~~ — **fatto, ultimo blocco del piano**. Nuova Cloud Function `suggestShoppingList` (output strutturato come `suggestIncomeDistribution`, per lo stesso motivo: il client aggiunge gli articoli alla lista solo dopo conferma esplicita — "Aggiungi tutti alla lista"). Il riepilogo passato all'AI usa i prezzi medi storici reali per prodotto (nuovo campo `averagePrice` in `_Suggestion` di `shopping_list_screen.dart`), mai inventati per i prodotti noti; normalizzazione server-side che garantisce che il totale non superi mai il budget anche se il modello sbaglia. **Limite noto:** con `gemini-3.5-flash-lite` il modello a volte "aggiusta" leggermente il prezzo stimato di un prodotto storico invece di escluderlo per rientrare nel budget, nonostante l'istruzione esplicita di non farlo — impatto reale nullo perché solo i *nomi* dei prodotti vengono salvati nella lista, mai i prezzi stimati, e il totale resta comunque garantito dalla normalizzazione server-side. Bottone "Lista con budget" (icona AI) nell'AppBar di `shopping_list_screen.dart`, solo Premium/Trial. Il riepilogo passato alla chat (`ai_chat_screen.dart`) ora include anche lo stato "on track" degli obiettivi (`Challenge.isOnTrack`, Blocco 6) e il trend delle abitudini (`HabitInsights.buildSummary`, Blocco 5) — stessa Cloud Function `chatWithAssistant` di prima, nessuna nuova funzione, solo un riepilogo più ricco.

   Piano completo con la tabella di classificazione Dart/Firestore vs Gemini per ogni punto: `/home/alessandro/.claude/plans/binary-soaring-blanket.md`. **Tutti i 9 blocchi del piano AI Premium sono completati.**

1. **Fase 6a — Google Play Billing (plumbing pronto, in attesa di configurazione Play Console):** codice client (`lib/services/billing_service.dart`, integrato in `premium_screen.dart`) e Cloud Function di verifica server-side (`verifyPlayPurchase` in `functions/index.js`) già scritti, ma **non testabili né utilizzabili finché non fai tu, sul tuo account Google Play Console:**
   - decidere l'`applicationId` Android definitivo (oggi è ancora il placeholder `com.example.smart_budget` in `android/app/build.gradle.kts` — non l'ho toccato perché legarlo a un valore diverso è una scelta solo tua, e va coordinato con l'app Android già registrata su Firebase);
   - creare in Play Console un prodotto abbonamento (subscription) e comunicarmi il suo ID reale, da mettere al posto del placeholder `premium_monthly` in `lib/services/billing_service.dart`;
   - creare un service account con accesso alla Play Developer API e mettere il suo JSON in `functions/.env` come `GOOGLE_PLAY_SERVICE_ACCOUNT_JSON`, più `ANDROID_PACKAGE_NAME` con l'applicationId scelto sopra. Finché queste due variabili mancano, `verifyPlayPurchase` rifiuta esplicitamente la richiesta (non finge mai che un acquisto sia valido).

   Il trial simulato via Firestore (`FirestoreService.startTrial`) resta invariato come percorso di sviluppo/test in emulatore, in parallelo al bottone "Abbonati subito su Google Play" (visibile solo se lo store è disponibile, cioè non sul Web). **Qualunque test con acquisti reali o account/carte reali va fatto da te — io non posso e non devo eseguirlo.**
2. **Fase 6b — AdMob (plumbing pronto, in attesa di configurazione AdMob Console):** `lib/services/ad_service.dart` + `lib/widgets/free_ad_banner.dart` gestiscono un banner mostrato solo agli utenti Free (né Premium né Trial attivo), cablato in `bottom_nav_shell.dart` sopra la bottom navigation bar (visibile su tutte le tab). Usa gli **ID di test ufficiali di Google** (sicuri, non richiedono un account AdMob, non generano mai pubblicità vera) sia nel codice Dart sia nei manifest nativi (`android/app/src/main/AndroidManifest.xml`, `ios/Runner/Info.plist`). **Prima della pubblicazione** serve che tu crei un'app AdMob reale (Android, ed eventualmente iOS) con almeno un ad unit "Banner", e mi passi: App ID AdMob e ad unit ID banner. Finché restano gli ID di test, l'app mostra sempre banner di test — mai pubblicità vera.
3. **Fase 7 — Servizi esterni (più avanti, non prioritario):** moduli assicurazioni/energia come da business plan, solo dopo validazione con utenti reali
4. **Pubblicazione (Fase L) — solo elenco prerequisiti, non ancora iniziata:**
   - **Google Play Console:** account sviluppatore, scheda app, questionario contenuti/rating, form "Sicurezza dei dati" (obbligatorio, deve elencare: email, dati finanziari inseriti dall'utente, foto scontrino elaborate temporaneamente via Gemini, dati condivisi con i membri famiglia), assets store (icona, screenshot, descrizione).
   - **`applicationId` Android definitivo:** oggi è ancora `com.example.smart_budget` (verificato in `android/app/build.gradle.kts` e `android/app/google-services.json`) — è una tua decisione (vedi Fase 6a), e va coordinata con l'app Android già registrata su Firebase.
   - **Firma release reale:** `android/app/build.gradle.kts` usa ancora `signingConfigs.getByName("debug")` per il build type release — serve una vera chiave di firma (upload key) o Play App Signing prima di poter caricare un APK/AAB su Play Console.
   - **Icona app reale:** i file in `android/app/src/main/res/mipmap-*/ic_launcher.png` sembrano ancora quelli di default generati da Flutter, mai sostituiti con un'icona brandizzata.
   - **Ambiente di produzione Firebase:** ~~verificare che Firestore Rules e le Cloud Functions siano effettivamente deployate sul progetto reale~~ **fatto** (vedi "Deploy in produzione" in fondo alla sezione beta readiness qui sopra) — Rules e tutte le Cloud Function sono live su `smart-budget-13198`, `GEMINI_API_KEY` caricata automaticamente da `functions/.env` nel deploy. Resta da fare: un test reale di una chiamata AI in produzione (non ancora eseguito, ha un costo) e verificare `firestore.indexes.json` se in futuro servissero indici composti (nessuno richiesto ad oggi dalle query esistenti).
   - **Google Play Billing e AdMob reali:** prerequisiti per il subscription reale e per gli annunci reali, già dettagliati nelle Fasi 6a/6b sopra — bloccati in attesa tua.
   - **Test chiuso:** Play Console richiede, per un account sviluppatore nuovo, almeno 12 tester continuativi per 14 giorni sul canale di test chiuso prima di sbloccare la pubblicazione in produzione — serve una build funzionante contro Firebase reale (non emulatori) da distribuire a quei tester.
   - **Privacy policy:** documento non ancora scritto, da pubblicare su un URL pubblico (richiesto sia dal form Play Console sia dal contenuto della scheda store) — deve coprire i dati raccolti, Gemini/Firebase/AdMob/Play Billing come trattamenti terzi, diritti dell'utente (probabile ambito GDPR).

## Beta readiness — verifica e messa in sicurezza (in corso)

Percorso a fasi (FASE 1-11) per portare l'app a una versione pronta per 10-20 beta tester, dal punto di vista funzionale/sicurezza/dati/business logic. Non è un audit generale né un rifacimento: si interviene solo dove serve, senza toccare scanner scontrino, riconoscimento prodotto, chiave Gemini, redesign Home (esclusi esplicitamente).

**FASE 1 — Controllo di stato (completata).** Verificate tutte le aree richieste (Famiglia/permessi, Firestore Rules, buste/transazioni, obiettivi/sinking funds/challenge, statistiche/previsioni, AI assistant/distribuzione stipendio, Free/Trial/Premium, billing/ads/analytics/persistenza/account). Sintesi delle lacune reali trovate all'epoca (oltre ai problemi di sicurezza, risolti in FASE 2 — vedi sotto). **Nota (2026-09-01): rileggendo questa lista oggi, ogni voce risulta ormai risolta nelle fasi successive — lasciata qui solo come cronologia, con lo stato reale attuale segnato punto per punto invece di ripeterla altrove:**
- Famiglia: manca la rimozione di un membro (nessuna Cloud Function/UI) — **risolto in FASE 3** (`removeFamilyMember`). `FamilyIncome` non salva le allocazioni verso le buste — **risolto in FASE 3** (campo `allocations`). Nessuna categoria di spesa personalizzabile per membro — **valutato e scartato deliberatamente in FASE 3** (non un gap dimenticato: scelta esplicita motivata, vedi sotto).
- Transazionalità: solo `addFamilyExpense` (`family_service.dart`) usava già `runTransaction`. A rischio race condition (read-then-write non atomico): `addExpense`, `addIncome`, `distributeIncome`, `addToChallenge` (tutte in `firestore_service.dart`) e soprattutto `addFamilyIncome` (`family_service.dart`, più membri scrivono sulle stesse buste condivise) — **risolto in FASE 4** (batch + `FieldValue.increment`, verificato con test di concorrenza reale sull'emulatore).
- Buste: non si poteva modificare una busta esistente (solo crea/elimina) — **risolto in FASE 4** (`updateEnvelope`/`updateFamilyEnvelope`, verificato presente in `firestore_service.dart` e cablato in `new_envelope_screen.dart`). Il campo "categoria" busta resta solo il nome duplicato, non un vero sistema di categorie — **non affrontato**, nessuna fase successiva lo ha toccato.
- Sinking Funds: il collegamento `Challenge.envelopeId` era solo visivo — **risolto in FASE 5** (il saldo busta è ora l'unica fonte di verità per il progresso).
- Challenge: nessun template "52 settimane" — **risolto in FASE 5**.
- Statistiche: mancavano "spese per busta" e un grafico andamento storico multi-mese in `analysis_screen.dart`; dashboard famiglia priva di entrate/categorie per membro; "budget giornaliero residuo" non calcolato tra le previsioni senza AI — **tutto risolto in FASE 6** (categorie per membro risolte come ripartizione per busta, stessa scelta motivata di FASE 3, non un vero campo categoria).
- Account: mancava completamente il **logout** e il **recupero password** in `login_screen.dart`; errori Firebase Auth mostrati col messaggio inglese grezzo, non tradotti — **tutto risolto in FASE 11** (verificato ora nel codice: `home_screen.dart` ha un `Drawer` con `FirebaseAuth.instance.signOut()`, `login_screen.dart` ha `sendPasswordResetEmail` + link "Password dimenticata?" e la traduzione dei codici errore).
- Billing: `restorePurchases()` esisteva in `billing_service.dart` ma non era cablato a nessun bottone in `premium_screen.dart` — **risolto in FASE 9** (verificato ora nel codice: bottone presente in `premium_screen.dart`, chiama `_restorePurchases` → `_billing.restorePurchases()`). Nessun ricontrollo periodico dello stato abbonamento — **parzialmente risolto in FASE 9**: `BillingService.reverifyStoredSubscription` riverifica presso Play ad ogni apertura di `PremiumScreen`, ma resta un limite consapevole, non un vero controllo periodico in background (richiederebbe Cloud Scheduler, mai introdotto senza decisione esplicita).
- Analytics: `firebase_analytics` non era nemmeno tra le dipendenze — **risolto in FASE 9** (`analytics_service.dart`, 13 eventi cablati; non verificabile in emulatore, solo con build reale contro Firebase vero).
- Confermato invece solido, nessun intervento necessario: Free/Trial/Premium con controlli sempre lato server nelle Cloud Function AI; nessun dato finto/statico nell'AI o nelle Statistiche; nessun leakage di dati tra utenti/famiglie nell'AI; previsioni senza AI già gratuite su Free; persistenza dati tutta su Firestore (solo l'onboarding flag è giustamente locale); AdMob con soli ID di test; distribuzione entrate AI sempre a doppia conferma esplicita.

**FASE 2 — Firestore Rules + sicurezza (completata).** Trovate e corrette 3 falle reali in `firestore.rules`:
1. **Auto-iscrizione a famiglie altrui**: la regola di creazione di `families/{id}/members/{memberId}` accettava anche `request.auth.uid == memberId`, permettendo a chiunque conoscesse un `familyId` di auto-aggiungersi come membro (anche con `role: "owner"`) bypassando l'intero flusso di invito, con accesso pieno a buste/spese/entrate della famiglia. **Corretto**: ora `allow create: if isOwner()` — l'owner che crea la propria famiglia resta coperto perché a quel punto il documento famiglia esiste già con `ownerId` == suo uid.
2. **`isPremium`/contatori d'uso AI scrivibili liberamente dal client** su `users/{uid}`: le Cloud Function leggono questi campi fidandosi ciecamente, ma il client poteva scriverli direttamente senza passare da `verifyPlayPurchase`. **Corretto**: `firestore.rules` ora blocca in scrittura dal client `isPremium`, `trialEnd`, `scontriniUsati`, `richiesteAiUsate`, `analisiAvanzateUsate`, `playProductId`, `playPurchaseToken`, `playSubscriptionExpiry` (scrivibili solo da Cloud Function via Admin SDK). Questo però rompeva `FirestoreService.startTrial()`, che scriveva questi campi direttamente dal client per il trial simulato (unico modo per i beta tester di provare Premium finché Play Billing reale non è configurato) — **spostato in una nuova Cloud Function `startTrial`** (`functions/index.js`, stesso comportamento di prima: 15 giorni, contatori azzerati), richiamata da `firestore_service.dart` via `cloud_functions`. Scelta confermata esplicitamente dall'utente tra le alternative proposte.
3. (minore) `invites/{id}`: il destinatario poteva modificare qualunque campo dell'invito, non solo lo stato. **Corretto**: `allow update` ora richiede `affectedKeys().hasOnly(['status'])`.

Verificato dopo le modifiche: `npm run lint` in `functions/` pulito, sintassi `firestore.rules` valida (avviata con successo nell'emulatore Firestore), `flutter analyze` senza nuovi errori (solo warning preesistenti non collegati, es. `withOpacity` deprecato).

**FASE 3 — Famiglia + membri + permessi + spese/entrate per membro (completata).**
1. **Rimozione membro** (mancava del tutto): nuova Cloud Function `removeFamilyMember` (`functions/index.js`) — solo l'owner può rimuovere un membro (verificato server-side, non solo nascosto in UI), l'owner non può rimuovere se stesso (per farlo servirebbe un flusso di eliminazione/uscita famiglia, non richiesto ora), cancella `families/{id}/members/{memberId}` e pulisce `users/{memberId}.familyId` via Admin SDK (l'utente rimosso non potrebbe più scriverlo da solo una volta perso l'accesso). Esposta in `family_service.dart` (`removeMember`) e in UI in `family_screen.dart` (icona di rimozione sulla card membro, visibile solo all'owner, con conferma). Le spese/entrate già registrate a nome del membro rimosso restano nello storico (comportamento voluto, coerente con come già funziona l'eliminazione di una busta).
2. **`FamilyIncome` non salvava le allocazioni verso le buste**: aggiunto il campo `allocations` al modello (`lib/models/family_income.dart`) e alla scrittura in `family_service.dart` (`addFamilyIncome`) — retrocompatibile, i documenti vecchi senza il campo tornano una mappa vuota. Ora è possibile ricostruire a posteriori dove è finita un'entrata familiare, come già avviene per le entrate personali.
3. **Entrate per membro assenti dalla dashboard famiglia**: aggiunti `familyTotalIncomes`/`memberIncomeTotal` in `family_service.dart` e una card "Entrate" in `family_dashboard_screen.dart` che rispetta lo stesso selettore Famiglia/Membro già usato per le spese.
4. **Categorie di spesa personalizzate per membro**: valutato e scartato deliberatamente. La "categoria" nell'app, sia per le spese personali (`new_expense_screen.dart`) sia per quelle familiari, non è oggi un vero campo strutturato — per le spese personali coincide semplicemente con la descrizione, per quelle familiari è ereditata dalla busta collegata. Aggiungere un campo categoria libero solo alle spese familiari avrebbe introdotto un pattern dati incoerente col resto dell'app invece di risolvere un problema reale: le spese "personali" di un membro (es. ceretta, parrucchiere) restano già distinguibili tramite `FamilyExpenseType.personal` + `memberId` + descrizione libera + busta collegata, che è lo stesso livello di strutturazione già in uso ovunque nell'app.

Verificato: `flutter analyze` senza nuovi errori, `npm run lint` in `functions/` pulito, e un test funzionale reale (non solo lint) di `removeFamilyMember`/`startTrial` sull'emulatore Firestore — chiamate dirette alle Cloud Function con `.run()`, coprendo: rimozione rifiutata per non-owner, rifiutata per estraneo, rifiutato tentativo di auto-rimozione owner, rimozione valida con verifica che il documento membro sparisca e `users/{uid}.familyId` si pulisca, rimozione doppia rifiutata (`not-found`), `startTrial` rifiutato senza auth e funzionante con auth (contatori azzerati, trialEnd a 15 giorni).

**FASE 4 — Buste + transazioni + distribuzione entrate (completata).**
1. **Operazioni non transazionali** (rischio race condition individuato in FASE 1): corrette tutte le funzioni che leggevano un saldo e poi lo riscrivevano separatamente. Invece di transazioni Firestore vere e proprie, usato un pattern più semplice ed equivalente — `WriteBatch` + `FieldValue.increment()` — che non richiede nemmeno una lettura preventiva del saldo (il delta è già noto lato client) ed è comunque atomico lato server:
   - `FirestoreService.addExpense`: spesa + decremento saldo busta in un unico batch.
   - `FirestoreService.addIncome` e `distributeIncome`: entrata + incremento saldo/i busta/e in un unico batch (`distributeIncome` risulta non richiamata da nessuna schermata — dead code preesistente, lasciata al suo posto e solo resa coerente, non è stata rimossa perché non è nello scope di questa fase).
   - `FirestoreService.addToChallenge`: `FieldValue.increment` diretto, nessuna lettura preventiva.
   - `FamilyService.addFamilyIncome`: stesso pattern batch, più rilevante qui perché più membri scrivono sulle stesse buste condivise. `FamilyService.addFamilyExpense` era già transazionale correttamente (Blocco precedente) e non è stata toccata.
   - **Verificato con un test reale sull'emulatore Firestore** (non solo lint): 20 "spese" da 10€ lanciate in concorrenza sulla stessa busta con il nuovo pattern hanno prodotto il saldo corretto (300/300, tutte e 20 le spese scritte); lo stesso scenario riprodotto con il vecchio pattern leggi-poi-scrivi ha perso 19 decrementi su 20 (saldo finale 490 invece di 300) — la correzione risolve un bug concreto, non solo teorico.
2. **Modifica busta assente** (solo crea/elimina prima): aggiunta `FirestoreService.updateEnvelope` e, in parallelo, `FamilyService.updateFamilyEnvelope`/`deleteFamilyEnvelope` (la busta familiare non aveva né modifica né eliminazione). Il saldo viene spostato della stessa differenza di budget (`budgetDelta = nuovo budget - vecchio budget`) così che "quanto è già stato speso" resti invariato dopo la modifica, invece di apparire come speso/risparmiato solo per effetto della modifica al budget. `NewEnvelopeScreen`/`NewFamilyEnvelopeScreen` ora accettano un parametro opzionale `envelope` che attiva la modalità modifica riusando lo stesso form, invece di duplicare gli schermi. Icona di modifica in `buste_screen.dart`; tap sulla card o icona elimina in `family_dashboard_screen.dart`.
3. **Distribuzione entrate**: già verificata completa in FASE 1 (doppia conferma esplicita, nessun movimento automatico) — nessun intervento necessario oltre alla correzione transazionale del punto 1.

Verificato: `flutter analyze` senza nuovi errori (stessi 33 preesistenti), `updateEnvelopeBalance` (diventata inutilizzata dopo il refactor di `addExpense`/`addIncome`/`distributeIncome` a batch) rimossa perché senza più alcun chiamante.

**FASE 5 — Obiettivi + Sinking Funds + Challenge (completata).**
1. **Sinking Funds "solo etichetta"** (segnalato in FASE 1: accantonare non spostava il saldo della busta collegata): scelta tecnica sottoposta all'utente tra tre alternative, confermata "il saldo busta diventa l'unica fonte di verità". Implementato in `challenge_screen.dart`: per una challenge di risparmio collegata a una busta, `Challenge.copyWithSavedAmount` (nuovo, in `lib/models/challenge.dart`) sostituisce a runtime il `savedAmount` con `Envelope.balance` prima di calcolare percentuale, quota mensile e badge "in linea col piano" — il contatore `savedAmount` salvato su Firestore per questi casi non viene più letto per il progresso (resta comunque nel documento, innocuo, congelato al valore che aveva). Il bottone "+ Aggiungi risparmio" è sostituito da una nota che indirizza l'utente a distribuire un'entrata verso quella busta, l'unico modo in cui il saldo si muove davvero. Gli obiettivi generici senza busta collegata e i tetti di spesa non cambiano comportamento.
2. **Challenge "52 settimane" assente** (nessun template, solo obiettivo manuale): aggiunto un bottone template nel form "Nuova challenge" che precompila titolo, obiettivo (€1.378, versione classica: €1 la 1ª settimana fino a €52 la 52ª) e scadenza a 364 giorni — restano comunque tutti modificabili prima di creare la challenge, riusando il form esistente invece di introdurre un nuovo tipo di dato/schermata.
3. **Obiettivi di risparmio**: nessun intervento necessario, già confermati completi in FASE 1 (importo, scadenza, quanto manca, percentuale, quota periodica, ritardo rispetto al piano, tutto su dati reali).

Verificato: `flutter analyze` senza nuovi errori (stessi 33 preesistenti). Nessuna Cloud Function o Firestore Rule toccata in questa fase.

**FASE 6 — Statistiche + previsioni (completata).** Tutte le lacune trovate in FASE 1 su `analysis_screen.dart`/`budget_insights.dart` colmate, sempre con dati reali da Firestore, mai valori statici:
1. **Entrate/spese/saldo del mese**: nuova riga di 3 stat tile in cima a `analysis_screen.dart` (nuovo `StreamBuilder<List<Income>>` annidato).
2. **Spese per busta**: nuova lista classificata (barra + importo) sotto "Spese per categoria", calcolata raggruppando le spese del mese per `envelopeId`.
3. **Andamento nel tempo**: nuovo grafico a barre (`_MonthlyTrendChart`, `fl_chart`) sugli ultimi 6 mesi, colore singolo (unica serie, nessuna legenda necessaria) con tooltip al tocco — seguita la skill `dataviz` per la scelta forma/colore invece di partire dai colori.
4. **Budget utilizzato/rimanente aggregato**: nuova riga di 3 stat tile (Budget totale / Utilizzato / Rimanente) riusando `FirestoreService.totalBudget/totalDisponibile/totalSpeso` già esistenti.
5. **Previsioni senza AI**: "spesa media giornaliera", "giorni rimanenti nel mese" e la nuova "budget giornaliero residuo" (`totalDisponibile / giorni rimanenti`) ora esposte come testo nella card "Previsione fine mese", non più calcolate e scartate.
6. **Famiglia — categorie per membro**: nuova card "Per busta" in `family_dashboard_screen.dart`, ripartizione per busta (usata come proxy di categoria, stessa scelta già motivata in FASE 3 per non introdurre un campo categoria incoerente) nello scope Famiglia/Membro selezionato.

Verificato: `flutter analyze` senza nuovi errori (stessi 33 preesistenti). **Non verificato visivamente in un browser/dispositivo reale**: in questo ambiente l'estensione Claude in Chrome non risulta connessa (nessun browser con l'estensione disponibile) e non sono installati strumenti di automazione (Puppeteer/Playwright/Selenium) per pilotare Chrome in headless — installarli per una verifica una tantum sarebbe stato sproporzionato. Ho verificato invece via lettura del codice che le nuove stat tile usano `Expanded` (larghezza vincolata, il `Text` va a capo invece di generare un overflow visivo in caso di importi molto larghi) e che il grafico ha un'altezza fissa con spazio riservato per le etichette. **Da fare quando possibile**: un giro manuale su dispositivo/emulatore reale della schermata Statistiche e della dashboard Famiglia per confermare che non ci siano overflow reali, specialmente su schermi stretti.

**FASE 7 — AI Assistant + AI consigli + AI distribuzione stipendio (completata).** FASE 1 non aveva trovato criticità qui (nessun dato finto, nessun leakage tra famiglie/utenti) — lavoro di rifinitura su una lacuna reale trovata rispetto a quanto richiesto:
1. **AI distribuzione stipendio non usava storico spese/entrate** (richiesti esplicitamente): `new_income_screen.dart` ora arricchisce il riepilogo passato a `suggestIncomeDistribution` con lo storico spese (riuso di `HabitInsights.buildSummary`, stesso identico calcolo già usato per la chat/l'analisi abitudini — nessun nuovo calcolo) e un riepilogo dello storico entrate (numero e importo medio delle entrate degli ultimi 6 mesi, calcolato inline). Il prompt lato server (`functions/index.js`) ora istruisce esplicitamente il modello a tenerne conto quando rilevante. Nessuna modifica alla firma della Cloud Function: il campo `summary` era già testo libero.
2. **Distribuzione entrate familiare priva di consiglio AI** (annotato in CLAUDE.md come "da valutare in futuro", punto ora affrontato): aggiunto lo stesso "Consiglio AI per la distribuzione" anche in `new_family_income_screen.dart`, riusando la stessa Cloud Function `suggestIncomeDistribution` (già generica, accetta qualunque lista di buste) con buste familiari, storico spese famiglia (`FamilyInsights.buildSummary`, stesso helper già usato per l'analisi AI famiglia) e storico entrate famiglia. L'accesso Premium/Trial resta verificato sull'utente personale che lo richiede, non sulla famiglia (stesso pattern già in uso per `_FamilyAnalysisCard`), coerente con "l'AI non deve mai accedere a dati di altri utenti/famiglie" perché ogni utente vede solo la propria famiglia tramite le Firestore Rules già in vigore. Stessa doppia conferma esplicita (Applica → Distribuisci) della versione personale, nessun movimento automatico.
3. **"Spese ricorrenti"** (elencate nella richiesta tra i dati che l'AI dovrebbe poter analizzare): confermato che non esistono nel modello dati dell'app (nessun campo `isRecurring`/simile su nessuna spesa) — non è una lacuna dell'AI ma l'assenza della funzionalità sottostante. Introdurla richiederebbe una feature a sé (tracciamento spese ricorrenti), fuori scope per una rifinitura AI: segnalato qui invece di costruirlo silenziosamente.

Verificato: `npm run lint` in `functions/` pulito, `flutter analyze` con solo 1 nuovo warning `withOpacity` deprecato (stesso pattern già usato ovunque nel resto del progetto, non un problema introdotto) — 34 issue totali invece di 33.

**FASE 8 — Free / Trial / Premium (completata, nessuna modifica di codice).** FASE 1 aveva già confermato il modello solido (controlli sempre lato server). Questa fase è stata una riverifica, dato che le FASE 2-7 hanno aggiunto nuove Cloud Function (`startTrial`, `removeFamilyMember`) e nuove funzionalità AI (distribuzione entrate familiare):
- Confermato via grep mirato che tutte le 5 Cloud Function che chiamano Gemini (`chatWithAssistant`, `scanReceipt`, `generateAiInsight`, `suggestIncomeDistribution`, `suggestShoppingList`) hanno ancora intatti auth check + `requireActiveAccess` + controllo quota dove previsto — nessuna toccata in modo da indebolire il gate.
- Confermato che i limiti trial sono ancora allineati manualmente tra client e server: `AppUser.trialMaxScontrini/trialMaxRichiesteAi/trialMaxAnalisi` (30/50/10) == `functions/index.js` (30/50/10) — nessun drift.
- Confermato che le funzionalità aggiunte nelle FASE 3-6 (rimozione membro, modifica busta, template 52 settimane, nuove card Statistiche) sono tutte a costo zero (Dart/Firestore) e correttamente NON gated dietro Premium, coerente con "Free non fa mai chiamate AI" — e che la nuova AI di distribuzione entrate familiare (FASE 7) verifica correttamente l'accesso sull'utente che la richiede.
- **Verificato con un test funzionale reale** (non solo lettura del codice) sull'emulatore Firestore, su `generateAiInsight` e `suggestIncomeDistribution`: chiamate dirette con `.run()` per utente non autenticato (rifiutato `unauthenticated`), Free (rifiutato `permission-denied`), trial scaduto (rifiutato `permission-denied`), trial con quota esaurita (rifiutato `resource-exhausted`), trial sotto quota (gate superato, poi fallito per API key non valida — atteso, nessuna chiave Gemini vera in questo test) e Premium (gate superato allo stesso modo). Tutti gli 11 casi si sono comportati come previsto.

**FASE 9 — Analytics + preparazione Ads + Billing (completata).**
1. **Firebase Analytics assente** (nessuna dipendenza, nessun evento — lacuna totale trovata in FASE 1): aggiunta la dipendenza `firebase_analytics` (`flutter pub add`) e un nuovo `lib/services/analytics_service.dart` con un metodo dedicato per ciascuno dei 13 eventi richiesti (apertura app, registrazione, login, creazione busta, spesa, entrata, utilizzo AI, utilizzo scanner, utilizzo lista spesa, creazione famiglia, invito membro, attivazione trial, acquisto Premium), cablati nei rispettivi punti (`main.dart`, `login_screen.dart`, `firestore_service.dart`, `family_service.dart`, `ai_service.dart`, `shopping_list_screen.dart`, `billing_service.dart`). Ogni evento passa solo nome + al massimo un tag strutturale non sensibile (es. `scope: personal|family`, `feature: chat|scan_receipt|...`) — **mai importi, descrizioni, categorie testuali o altri dati finanziari/personali nei parametri**, verificato metodo per metodo in `analytics_service.dart`. L'evento "acquisto Premium" è loggato solo per un acquisto nuovo (`PurchaseStatus.purchased`), non per un ripristino, per non gonfiare il conteggio ad ogni reinstall.
   **Non verificabile in questa sessione**: non esiste un emulatore locale per Firebase Analytics (a differenza di Auth/Firestore/Functions) — i dati arrivano solo se l'app gira contro il progetto Firebase reale con Google Analytics collegato. Verificato però con una build reale (`flutter build web --release`, l'unico target compilabile in questo ambiente senza Android SDK) che tutto il codice compila correttamente con la nuova dipendenza.
2. **Bottone "Ripristina acquisti" mai cablato**: aggiunto in `premium_screen.dart`, chiama `BillingService.restorePurchases()` (già scritta ma inutilizzata) e riusa il flusso di verifica server-side già esistente.
3. **Nessun ricontrollo periodico dello stato abbonamento**: aggiunta `BillingService.reverifyStoredSubscription` (refactor che estrae la chiamata a `verifyPlayPurchase` già usata da `_verifyWithServer`, zero duplicazione) e due nuovi campi letti (mai scritti) dal client su `AppUser`: `playPurchaseToken`/`playProductId`, già scritti da `verifyPlayPurchase` ma mai riletti finora. `PremiumScreen` ora, ad ogni apertura, se l'utente risulta già Premium con un token salvato, lo riverifica presso Play invece di fidarsi ciecamente dell'ultimo stato scritto — così una cancellazione/scadenza nel frattempo viene rilevata al prossimo accesso alla schermata, non solo se lo store ripresenta spontaneamente un evento (che per un abbonamento scaduto non è garantito). **Limite consapevole**: il ricontrollo scatta solo quando l'utente apre la schermata Premium, non è un controllo periodico in background — un vero controllo periodico per tutti gli utenti richiederebbe un Cloud Scheduler lato server (nuova infrastruttura, implicazioni di fatturazione GCP), che non ho introdotto senza una tua decisione esplicita.
4. **Pubblicità (AdMob)**: nessun intervento necessario, già confermato completo in FASE 1 (solo ID di test, corretto gating Free-only).

Verificato: `flutter analyze` con solo lo stesso set di warning preesistenti (34, nessuno nuovo), build web reale completata con successo con tutte le modifiche di questa fase.

**FASE 11 — Test completo e correzione bug (completata; FASE 10 Multilingua saltata su tua richiesta, non ancora iniziata).**

1. **Account (sezione 22, mai assegnata a una fase specifica — gap reali trovati in FASE 1)**:
   - **Logout mancante**: l'icona hamburger nell'AppBar della Home esisteva già ma era puramente decorativa (nessun `onPressed`, nessun `Drawer` collegato). Collegata a un nuovo `Drawer` minimale (email utente + "Esci") in `home_screen.dart` — non è un redesign della Home, l'aspetto della schermata resta identico, si tratta di un pannello separato che prima non si apriva.
   - **Password dimenticata assente**: aggiunto `sendPasswordResetEmail` + link "Password dimenticata?" in `login_screen.dart`.
   - **Errori Firebase Auth in inglese grezzo**: aggiunta una traduzione dei codici più comuni (`user-not-found`, `wrong-password`, `invalid-email`, `email-already-in-use`, `weak-password`, `too-many-requests`, `network-request-failed`, ecc.) in italiano comprensibile.
   - **`login_screen.dart` non allineata al design system**: allineata (AppBar-equivalente bianca, campi filled/rounded, bottone primario pieno arrotondato) dato che la schermata andava comunque riscritta sostanzialmente per i punti sopra — aggiunta alla lista "Schermate allineate" in cima a questo file.
2. **Verifica reale interattiva** (non solo `flutter analyze`): in questo ambiente senza estensione Claude in Chrome connessa, ho installato temporaneamente Playwright in un venv isolato nello scratchpad (non aggiunto al progetto) per pilotare Chrome via CDP e testare l'app vera contro gli emulatori Firebase, viewport 390×844 (larghezza tipica smartphone). Percorso testato end-to-end con screenshot ad ogni passo: onboarding → registrazione → Home (drawer/logout) → creazione busta → **modifica busta con verifica che saldo e budget si spostino insieme** (FASE 4) → registrazione spesa con **verifica reale che il saldo si decrementi atomicamente** → Statistiche (tutte le nuove card/grafici FASE 6, nessun overflow) → creazione famiglia → dashboard famiglia (card Entrate ed Per busta, FASE 3/6) → busta e spesa familiare (saldo aggiornato correttamente) → **Sinking Fund collegato a una busta: confermato che il progresso mostrato coincide esattamente col saldo reale della busta, bottone "+ Aggiungi risparmio" correttamente sostituito dalla nota informativa** (FASE 5) → template "Sfida delle 52 settimane" (valori precompilati corretti) → schermata Premium → **attivazione trial tramite la nuova Cloud Function `startTrial`, confermato che sblocca davvero l'accesso AI in tempo reale** (conferma pratica della modifica di sicurezza della FASE 2). Nessun overflow, nessun crash, nessun errore di layout in nessuna delle schermate toccate in questa sessione.
3. **`flutter test`**: passa pulito (vedi nota aggiornata in "Problemi noti" sopra).
4. **`flutter build apk --release`**: non eseguibile in questo ambiente (nessun Android SDK installato, confermato da `flutter doctor`) — verificato invece con `flutter build web --release`, l'unico target compilabile qui, completato con successo più volte durante la sessione.
5. **Sezioni 19 (persistenza) e 20 (aggiornamenti futuri)**: nessun intervento necessario — persistenza già confermata solida in FASE 1, e tutti i campi Firestore aggiunti in questa sessione (`allocations`, `createdAt`, `playPurchaseToken`, `playProductId`, ecc.) sono opzionali/retrocompatibili con i documenti esistenti, mai una migrazione distruttiva.
6. **Sezione 23 (lista della spesa)**: nessun intervento necessario, integrazione con lo scanner già confermata funzionante da una sessione precedente, non toccata.

**Deploy in produzione (confermato esplicitamente dall'utente, un'unica volta a fine FASE 3-9+11):** `firebase deploy --only firestore:rules,functions` eseguito con successo sul progetto reale `smart-budget-13198`. Firestore Rules pubblicate (correzioni di sicurezza FASE 2 ora live). Cloud Function: **create per la prima volta** (non erano mai state deployate finora, solo testate su emulatore) `generateAiInsight`, `suggestIncomeDistribution`, `suggestShoppingList`, `removeFamilyMember`, `startTrial`, `verifyPlayPurchase`; **aggiornate** `chatWithAssistant`, `scanReceipt`, `inviteFamilyMember`, `acceptFamilyInvite`. Il deploy ha caricato automaticamente `functions/.env` come variabili d'ambiente delle function (comportamento standard Firebase per le funzioni 2ª generazione — confermato nel log "Loaded environment variables from functions/.env"), quindi la chiave Gemini reale già presente in quel file dovrebbe ora essere attiva anche in produzione, non solo in locale — **non verificato con una chiamata reale** (avrebbe un costo e creerebbe dati reali, non l'ho fatto senza chiedere). Questo soddisfa presumibilmente il prerequisito "GEMINI_API_KEY configurata nell'ambiente Cloud Functions di produzione" elencato più sotto in Fase L — da confermare con un test reale quando vorrai.

## Onboarding avanzato — demo interattiva + wizard di configurazione reale (completato)

Sostituisce il vecchio `OnboardingScreen` (4 pagine titolo+testo+icona+pulsante, `lib/screens/onboarding_screen.dart`, ora eliminato) con due pezzi distinti, entrambi mostrati solo una volta per dispositivo/account e sempre skippabili:

1. **`OnboardingFlow`** (`lib/screens/onboarding/onboarding_flow.dart`) — mini demo interattiva di 11 pagine mostrata prima ancora del login (`_RootGate` in `main.dart`, stesso punto di prima, stesso flag `OnboardingService`/`shared_preferences`). Ogni pagina FA VEDERE come funziona una parte dell'app (Home, buste, spese, scanner scontrino, lista della spesa, statistiche, obiettivi, famiglia, AI chat, Premium) con dati statici ed espliciti, definiti in `lib/data/onboarding_demo_data.dart` — **nessuna scrittura su Firestore, nessuna chiamata AI, nessun account richiesto**. I widget di supporto (card demo, contatori animati, ingressi ritardati) sono in `lib/widgets/onboarding/onboarding_widgets.dart` e `onboarding_animations.dart`, riusabili tra le pagine demo e il wizard reale.
2. **`SetupFlow`** (`lib/screens/onboarding/setup/`) — wizard di configurazione reale mostrato **solo** a un account appena creato dalla registrazione (mai a un account esistente, mai a un login): nome, entrata mensile (`SetupIncomeStep`, salvata con `FirestoreService.addIncome` se >0, saltata senza scrivere nulla se lasciata a 0), buste con budget suggeriti proporzionati all'entrata inserita (`SetupEnvelopesStep`, creazione reale via `FirestoreService.addEnvelope`), obiettivo di risparmio opzionale (`SetupGoalStep`, `FirestoreService.addChallenge`), infine `SetupTransformScreen` che rilegge i dati veri appena scritti e li anima in una mini Home prima del bottone "INIZIA". Ogni passaggio scrive con gli stessi servizi già usati altrove nell'app (nessuna logica di scrittura duplicata) e va solo in avanti (nessun "indietro" — tornare indietro richiederebbe disfare scritture reali già avvenute, deliberatamente non implementato).
   - **Gate:** nuovo campo `setupCompleted` (bool) su `users/{uid}`, scritto `false` da `login_screen.dart` al momento della registrazione (mai su un login), letto da `FirestoreService.streamSetupCompleted()` (default `true` se il campo manca, così un account esistente creato prima di questa funzionalità non vede mai il wizard). `_RootGate` in `main.dart` mostra `SetupFlow` finché il flag non è vero; `SetupTransformScreen.markSetupCompleted()` lo marca vero al tap su "INIZIA", e `_RootGate` (che riascolta lo stesso stream) passa da solo alla Home reale — nessuna navigazione manuale, nessuna seconda Home da mantenere allineata a quella vera.
3. **Bug pre-esistente trovato e corretto durante la verifica di questo blocco** (non introdotto da questo lavoro, presente identico già in `HEAD` prima di queste modifiche): l'`AppBar` di `home_screen.dart` (icona + "Spesa Intelligente" in un `Row` non protetto, con 3 icone azione a destra) andava in overflow orizzontale di meno di un pixel su viewport stretti (390px, iPhone/Android di fascia media) — invisibile quasi sempre ma un errore reale (`RenderFlex overflowed`), riprodotto in modo affidabile durante il test end-to-end di questo blocco. Corretto avvolgendo il `Text` in un `Flexible` con `overflow: TextOverflow.ellipsis` (`home_screen.dart`, riga della `title:` dell'AppBar) — ora tronca con puntini invece di sforare, verificato che l'errore non si ripresenta.
4. **Verificato:** `flutter analyze` pulito (stessi 31 warning preesistenti, nessuno nuovo), `flutter build web --release` e `flutter test` completati con successo. Verifica interattiva reale end-to-end contro gli emulatori Firebase (stesso approccio Playwright isolato nello scratchpad già usato in FASE 11, dato che l'estensione Claude in Chrome non risultava connessa in questa sessione): demo di onboarding completa (11 pagine, tasto "Salta" e avanzamento pagina per pagina) → atterraggio su Login → registrazione nuovo account → `SetupFlow` reale (nome → entrata → buste con budget proporzionati → obiettivo "Non ancora" → transform screen con i dati veri appena scritti) → tap "INIZIA" → Home reale (`BottomNavShell`) con saluto, buste e saldo corretti e nessun errore in console. Non verificato: build Android reale (nessun Android SDK in questo ambiente, stesso limite di sempre) e comportamento su tablet/schermi molto larghi (fuori scope, mai richiesto).

## Blocchi post-beta (2026-09-02, in corso — un blocco alla volta, riepilogo a fine di ognuno)

**Block 1 — piccole correzioni onboarding/naming (completato).** Basso rischio, eseguito in autonomia:
- Demo onboarding famiglia (`onboarding_demo_data.dart`): nomi aggiornati a "Alessandro (Padre)", "Selene (Moglie)", "Paolo (Figlio)" — ruolo incluso nello stesso campo nome (mostrato già affiancato dalla card esistente in `demo_page_famiglia.dart`), nessuna modifica al modello dati statico.
- Calendario: verificato che **non esiste alcuna schermata Calendario** nel progetto — solo due icone calendario puramente decorative (`setup_goal_step.dart`, `home_screen.dart`), nessun testo inglese da localizzare. Non creata alcuna nuova schermata.
- Busta/categoria "Buffer" rinominata in "Fondo imprevisti" (`onboarding_demo_data.dart`, `setup_envelopes_step.dart`) — solo dati demo/suggerimento, nessuna busta reale utente esistente da migrare.

**Block 2 — busta/categoria facoltative per le spese: "Spese generali" (completato).** Con la busta generale come opzione sempre selezionabile nel picker (mai un default silenzioso — coerente con la regola già confermata sullo Scanner, vedi memoria `scan-receipt-envelope-matching`):
- `Envelope.isGeneral` (nuovo campo, default `false`, retrocompatibile) + `FirestoreService.ensureGeneralEnvelope()`: crea al bisogno (al più una per utente) la busta "📦 Spese generali" con budget/saldo che partono da 0 e il saldo può scendere sotto zero come qualunque altra busta — nessun caso speciale nelle statistiche aggregate (`totalBudget`/`totalDisponibile`/`totalSpeso` restano corretti per costruzione) né negli alert budget (`budget_insights.dart` ignora già le buste con `budget <= 0`).
- `new_expense_screen.dart` e `scan_receipt_screen.dart`: il picker busta mostra sempre "📦 Spese generali" e "+ Nuova busta" (dialog inline, crea una busta reale al volo senza uscire dal form) accanto alle buste reali — la selezione resta comunque obbligatoria in entrambi i flussi.
- `new_envelope_screen.dart`: budget busta ora opzionale (rimosso il vincolo `budget <= 0`); `buste_screen.dart` mostra "Nessun budget impostato" invece di "€0,00" quando pertinente.
- Fuori scope per ora (non richiesto): stesso pattern per le spese familiari (`new_family_expense_screen.dart`).

**Block 2 — fix reale invito famiglia (completato, deployato in produzione).** Diagnosticato e corretto con un test end-to-end reale contro Firebase di produzione (due account veri), come richiesto esplicitamente per compensare l'impossibilità di validare le regole in anticipo sull'emulatore (mancava un JDK compatibile — **poi risolto**, vedi sotto).
- **Causa reale, trovata solo con il test dal vivo** (la prima ipotesi — indice Firestore mancante sulla query `collectionGroup('invites')` — era plausibile ma incompleta): un `match /families/{familyId}/invites/{inviteId}` **annidato** in `firestore.rules` governa correttamente `get`/`create`/`update`/`delete` su un documento preciso, ma **Firestore non lo applica mai a una query `collectionGroup()`** (usata da `streamMyPendingInvites()` in `family_service.dart` per mostrare gli inviti pendenti al destinatario) — qualunque condizione in quel blocco annidato, perfino `allow list: if true`, veniva ignorata e la query rifiutata con `permission-denied`, indipendentemente dai dati reali. Per una query `collectionGroup` serve un match dichiarato con `{path=**}` a livello superiore nel file delle regole. Questo bug preesisteva a qualunque modifica di questa sessione — l'invito non ha probabilmente mai funzionato.
- **Fix**: nuovo blocco `match /{path=**}/invites/{inviteId} { allow list: ... }` in cima a `firestore.rules`, con un confronto diretto `resource.data.invitedUid == request.auth.uid` (l'unica forma che Firestore riesce a "dimostrare" per una query list — un confronto sull'email, anche normalizzata, non è altrettanto affidabile). Aggiunto il campo `invitedUid` all'invito (scritto da `inviteFamilyMember`, che già risolve l'utente via `getAuth().getUserByEmail()`) — elimina anche alla radice, non solo lo patch, il problema di case-sensitivity dell'email originariamente segnalato (l'owner può digitare l'email invitata con qualunque maiuscole/minuscole). Il blocco annidato mantiene invariati `get`/`create`/`update`/`delete` (mai stati il problema). Indice Firestore composito aggiornato di conseguenza (`invitedUid`+`status`, scope collection group).
- **Verificato**: dopo aver scoperto che l'ambiente aveva in realtà un JDK 21 già installato (solo non di default — bastava puntare `JAVA_HOME`, nessuna installazione richiesta), l'intero fix è stato prima isolato e confermato sull'emulatore Firestore locale (creazione owner+membro reali, invito reale via la vera Cloud Function `inviteFamilyMember`, query `collectionGroup` come destinatario, accettazione con modifica solo di `status`, tentativo di manomissione di altri campi correttamente respinto) — mai stata usata una regola permissiva in `firestore.rules` stesso per questo test, solo in una copia isolata nello scratchpad, per non esporre mai la produzione a una finestra di regole deboli. Poi confermato di nuovo end-to-end **contro produzione reale** con due account veri (owner + membro), owner che invita il membro con email in maiuscolo diverso da come si è registrato: invito arrivato correttamente al membro, accettato, membro comparso nella lista membri della famiglia.
- **Nota per sessioni future**: l'emulatore Firestore/Auth/Functions locale ora funziona in questo ambiente (richiede solo `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64` prima di `firebase emulators:start`, dato che il Java di default del sistema è la 17) — usarlo per validare qualunque modifica a `firestore.rules` prima di un deploy in produzione, specialmente per query `collectionGroup`, invece di scoprire problemi solo con un test dal vivo come in questo caso.
- **Dati di test lasciati in produzione** (bassa priorità, da pulire quando comodo): due account reali (`budget.test.owner@example.com`, `budget.test.membro@example.com`, password `TestPass123!`) e una famiglia "Famiglia Test E2E" con buste demo standard, usati per il test end-to-end — nessun dato finanziario reale, isolabili/eliminabili in qualunque momento.

**Block 5 — notifiche push (completato e verificato in emulatore, deploy volutamente rimandato).** Non è un blocco esterno né un lavoro a metà: codice e test sono completi, il deploy in produzione (`firebase deploy --only firestore:rules,functions`) è stato deliberatamente rimandato a una decisione esplicita dell'utente su quando attivarlo — vedi il calcolo costi (Cloud Scheduler/Functions/Firestore trascurabili a qualunque scala realistica vicina, Gemini pochi centesimi/mese) discusso e accettato prima di lasciare il blocco in questo stato. Due canali distinti, coerenti col resto del progetto (Free zero costo via regole/statistiche, Premium/Trial con AI e stessa quota condivisa):
- **Client**: `lib/services/notification_service.dart` (`NotificationService`) chiede il permesso di notifica una sola volta per dispositivo (flag `shared_preferences`, mai al primissimo avvio — solo da `SetupTransformScreen._finish()` per un account nuovo, o al primo giro su `HomeScreen.initState()` per un account già esistente) e salva/aggiorna il token FCM su `users/{uid}.fcmToken`. `lib/screens/notifications_screen.dart` (`NotificationsScreen`) mostra lo storico completo (letto da `users/{uid}/notifications`, non solo la notifica di sistema che sparisce), raggiungibile dalla nuova icona campanella nell'AppBar di `home_screen.dart`. `web/firebase-messaging-sw.js` gestisce le push in background sul target Web/PWA (registrato automaticamente dal plugin al path di default, nessuna riga in più richiesta in `web/index.html`). `pubspec.yaml`: aggiunta `firebase_messaging`.
- **Server** (`functions/index.js`): nuova Cloud Function schedulata `dailyScheduledChecks` (`onSchedule`, ogni giorno alle 9:00 Europe/Rome) che itera tutti gli utenti e per ciascuno chiama `checkUserFreeAlerts` (busta esaurita/≥85% utilizzata, promemoria "nessuna spesa da 5 giorni" — stesse condizioni già mostrate nella card Home di `budget_insights.dart`, ricalcolate qui in JS perché devono girare anche per chi non apre l'app, non è una duplicazione accidentale) e `sendPremiumDailyTipPush` (consiglio del giorno via AI, solo per chi è Premium/Trial attivo e ha un token salvato, riusa la stessa cache giornaliera e la stessa quota condivisa `analisiAvanzateUsate` di `generateAiInsight` — se l'utente ha già aperto l'app oggi e il consiglio è già in cache, non viene rigenerato né consuma quota, solo inviato). `sendPushAndLog` scrive sempre la notifica su `users/{uid}/notifications` (storico consultabile anche senza push riuscita) e tenta l'invio FCM solo se esiste un token, ripulendo il token se Firebase segnala esplicitamente `messaging/registration-token-not-registered` (mai per un errore di rete generico). Ogni condizione ha una chiave di dedup salvata sull'utente (`notifiedBudgetAlerts` per mese+busta+condizione, `notifiedNoExpenseDateKey` per giorno) per non rimandare lo stesso avviso ad ogni esecuzione — necessario perché Cloud Scheduler consegna "at-least-once".
- **Regole**: nuova subcollection `users/{uid}/notifications` in `firestore.rules` — il client può leggere le proprie notifiche e aggiornare solo il campo `read` (mai crearle o eliminarle, sarebbero notifiche finte mai davvero inviate: solo l'Admin SDK, via `sendPushAndLog`, può scriverle).
- **Bloccato in attesa di configurazione esterna** (stesso pattern già in uso per Play Billing/AdMob in Fase 6a/6b): su iOS, `UIBackgroundModes: remote-notification` è già in `ios/Runner/Info.plist`, ma la ricezione reale in background richiede una chiave APNs configurata su Apple Developer + Firebase Console (non eseguibile da qui). Su Web, `getToken()` senza una **VAPID key** generata nella console Firebase (Cloud Messaging → Web configuration) può fallire silenziosamente — già gestito con un `try/catch` che non blocca il resto dell'app (nessun token salvato per quel dispositivo, nessuna push, ma l'app resta utilizzabile), coerente con "mai un default silenzioso che finge di funzionare". Su Android nessun blocco noto (FCM funziona senza configurazione aggiuntiva oltre a `google-services.json`, già presente).
- **Verificato** (emulatori Firestore/Auth/Functions/Pub-Sub locali, `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64`): utente Free reale seminato con una busta esaurita, una al 90% e una spesa di esattamente 5 giorni fa → trigger manuale di `dailyScheduledChecks` (nell'emulatore la funzione schedulata risponde su `dailyScheduledChecks-0`, non sul nome base) → le 3 notifiche attese scritte correttamente nello storico, con testo corretto. Un secondo trigger identico non ha duplicato nessuna delle 3 (dedup confermato). Regole confermate con lo stesso utente: lettura ok, `update({read: true})` accettato, `update` di un altro campo rifiutato (`permission-denied`), `create`/`delete` dal client rifiutati (`permission-denied`). Il percorso Premium (`sendPremiumDailyTipPush`) non è stato invocato per questo utente (né Premium né Trial), confermando che non ha chiamato Gemini né consumato quota — non testato con un utente Premium reale per evitare un costo AI non necessario, stesso approccio già usato per gli altri test di gate in FASE 8. `flutter analyze` pulito (stessi 31 warning preesistenti), `npm run lint` in `functions/` pulito, `flutter build web --release` completata con successo.
- **Non ancora fatto**: nessun handler per i messaggi ricevuti mentre l'app è in primo piano (`FirebaseMessaging.onMessage`) — su mobile/web il comportamento FCM di default non mostra un banner di sistema in quel caso; la notifica resta comunque scritta nello storico (`NotificationsScreen`) e visibile lì. Un pallino (non un conteggio) sull'icona campanella per notifiche non lette è stato aggiunto nel Block 6 sotto, insieme a un invito famiglia pendente. **Deploy in produzione volutamente rimandato** (`firebase deploy --only firestore:rules,functions`, non ancora eseguito): scelta esplicita dell'utente, non un blocco — attivare quando deciderà lui, nessun'altra condizione da soddisfare prima.

**Block 6 — Fase A del giro di correzioni post-audit (completato e verificato interattivamente).** Prima di questo blocco è stato fatto un audit punto per punto (richiesto dall'utente con una lista di ~20 voci) di cosa fosse già fatto vs da fare — vedi "Piano fasi D-H" più sotto in questo file per l'elenco completo delle fasi ancora da fare (D-H; le fasi A-C sono i Block 6/7/8). Lavoro autonomo dentro la fase, come da istruzione esplicita dell'utente ("fermati solo quando ci sono costi da accettare" — nessun costo in questa fase):
1. **Date/time picker in inglese**: `main.dart` non aveva mai `localizationsDelegates`/`supportedLocales` configurati, quindi `showDatePicker`/`showTimePicker` (es. scadenza obiettivo in `challenge_screen.dart`) mostravano sempre l'inglese, mai notato perché nessuno l'aveva controllato esplicitamente in una fase precedente. Aggiunta la dipendenza SDK `flutter_localizations` (`pubspec.yaml`, nessun costo: è parte dell'SDK Flutter) e configurato `locale: Locale('it')` + i tre delegate globali (Material/Widgets/Cupertino) in `MaterialApp` (`main.dart`).
2. **Indicatore quota rimanente invisibile nel momento in cui serve**: i contatori (`scontriniUsati`/`richiesteAiUsate`) esistevano già ma erano mostrati solo in `premium_screen.dart`. Nuovo widget riusabile `lib/widgets/trial_quota_badge.dart` (`TrialQuotaBadge`, si nasconde da solo per Free/Premium — solo il Trial ha un limite), cablato in `scan_receipt_screen.dart` ("Scontrini: X/30 rimasti questo mese") e `ai_chat_screen.dart` ("Richieste AI: X/50 rimasti questo mese"), con colore d'allarme quando restano ≤20%.
3. **Invito famiglia pendente invisibile**: prima si vedeva solo dentro `family_screen.dart`, in un testo piccolo, solo per chi non ha ancora una famiglia — nessun modo di scoprirlo altrimenti. Aggiunta `_NotificationsBellButton` in `home_screen.dart`: la campanella nell'AppBar mostra ora un pallino rosso quando c'è almeno un invito pendente (`FamilyService.streamMyPendingInvites()`) o una notifica non letta (`NotificationService.streamNotifications()`), combinando i due stream indipendenti solo per decidere se mostrare il pallino — l'elenco reale al tap resta `NotificationsScreen`.
- **Verificato**: `flutter analyze` pulito (stessi 31 warning preesistenti, nessuno nuovo), `flutter build web --release` completata con successo. **Verifica interattiva reale** (non solo analyze/build, dato che erano modifiche visive): emulatori Firestore/Auth locali, utente di test reale seminato con un invito famiglia pendente e una notifica non letta (Admin SDK, bypassando le regole come farebbe davvero `inviteFamilyMember`), login reale via `flutter run -d web-server` contro l'emulatore, screenshot ad ogni passo (Playwright, stesso approccio isolato nello scratchpad già usato in sessioni precedenti — le build CanvasKit di Flutter web non espongono testo DOM standard, quindi i click sono per coordinate lette dagli screenshot, non per selettore testo) — confermato: pallino rosso visibile sulla campanella in Home; aprendo "Nuova challenge" → "Scegli scadenza", il date picker mostra "Seleziona data", "dicembre 2026", intestazioni giorni "L M M G V S D", bottoni "Annulla"/"OK", tutto in italiano; badge "Scontrini: 3/30 rimasti questo mese" (colore arancio, sotto soglia) nello Scanner e "Richieste AI: 47/50 rimasti questo mese" (colore neutro) nella Chat AI, dopo aver attivato un trial di prova sull'utente.

**Block 7 — Fase B, parte 1: email di verifica account (completato e verificato).** Solo la parte senza costi della Fase B (email di verifica, gratuita — il sistema email integrato di Firebase Auth non richiede alcun provider esterno né configurazione). La seconda parte della Fase B (email quando arriva un invito famiglia) richiede invece un provider email esterno (es. Resend/SendGrid): proposta all'utente, **scartata esplicitamente per ora** ("Nessuno per ora") — l'invito resta visibile solo in-app (badge campanella già aggiunto nel Block 6), nessuna email. Da riconsiderare solo se l'utente lo chiederà di nuovo in futuro.
- `login_screen.dart`: `_signUp()` chiama `credential.user?.sendEmailVerification()` subito dopo la creazione dell'account (e dopo l'eventuale `updateDisplayName`) — non blocca in alcun modo l'accesso: un utente non verificato resta comunque operativo, coerente con "non spezzare gli account esistenti" (nessun account creato prima di questa modifica ha mai verificato l'email).
- `home_screen.dart`: nuovo `_EmailVerificationBanner`, card informativa (stile coerente, sfondo neutro) visibile in cima alla Home finché `FirebaseAuth.currentUser.emailVerified` è `false`, con due azioni: "Invia di nuovo" (richiama `sendEmailVerification()`) e "Ho verificato" (chiama `user.reload()` — l'unico modo per accorgersi che l'utente ha cliccato il link nel frattempo, Firebase non lo notifica in tempo reale).
- **Verificato**: `flutter analyze` pulito (31 warning preesistenti), `flutter build web --release` ok. Verifica interattiva reale: registrazione di un account vero contro l'emulatore Auth (Playwright, stesso approccio del Block 6) — confermato che `emailVerified` risulta `false` subito dopo la registrazione (controllato via Admin SDK) e che il banner "Verifica la tua email" con i due bottoni compare correttamente in Home al primo accesso.

**Block 8 — Fase C: persistenza login su iPhone PWA (completato, verificato solo fino al limite possibile in questo ambiente).** La sessione precedente è stata interrotta a metà (VS Code chiuso, processo terminato con `exit code 137`) subito dopo aver applicato la modifica e lanciato la riverifica — ripresa in una nuova sessione: `flutter analyze` (31 warning preesistenti, nessuno nuovo) e `flutter build web --release` rieseguiti da zero con esito pulito prima di documentare, nessuna modifica di codice ulteriore necessaria.
- **Causa probabile**: nessuna persistenza Firebase Auth esplicita era mai stata configurata — su web (incluso il target usato da una PWA aggiunta alla schermata Home su iOS) l'SDK sceglie da solo la persistenza migliore disponibile, ma può ripiegare in silenzio su "solo in memoria" (login perso ad ogni riapertura) in contesti dove IndexedDB è ristretto.
- **Fix**: `lib/main.dart` chiama esplicitamente `FirebaseAuth.instance.setPersistence(Persistence.LOCAL)` quando `kIsWeb`, subito dopo `Firebase.initializeApp()` — richiede la persistenza migliore invece di lasciarla al fallback automatico, evitando quel degrado silenzioso.
- **Limite noto, non risolvibile da qui**: Safari/WebKit su iOS ha una storia nota di trattare lo storage di una PWA aggiunta alla Home in modo separato e più volatile rispetto a Safari normale (ITP — Intelligent Tracking Prevention) — un limite della piattaforma, non del codice. Nessun dispositivo iOS reale disponibile in questo ambiente: il fix non è quindi verificabile end-to-end sul caso reale che ha originato la segnalazione, solo tramite lettura della documentazione Firebase/WebKit e verifica che la chiamata non introduca regressioni (build pulita, nessun cambiamento di comportamento su Android/desktop dove la persistenza di default è già adeguata). **Da confermare quando possibile**: riprovare la PWA su un iPhone reale dopo questo fix e riferire se il login persiste ora tra le riaperture.

**Piano fasi D-H (audit post-beta, ancora da fare — richiede il tuo via libera fase per fase, vedi regola sotto).** Nato da un audit punto-per-punto di una lista di ~20 richieste dell'utente (Fasi A-C sopra già completate come Block 6/7/8). Regola di lavoro concordata: dentro ogni fase si procede in autonomia senza fermarsi ad ogni singola modifica, ma **ci si ferma e si chiede il consenso esplicito PRIMA di iniziare ogni nuova fase**, spiegando cosa si farà e le eventuali decisioni aperte — mai iniziare la fase successiva di propria iniziativa. Aggiornare CLAUDE.md a fine fase, come per le fasi precedenti.
- **Fase D — spese fisse ricorrenti**: nuovo modello dati per spese ricorrenti (es. "Mutuo, €700, ogni mese il giorno 5"), UI per definirle/modificarle/eliminarle, meccanismo che le registra come spesa reale **sempre con conferma esplicita dell'utente**, mai automatico/silenzioso.
- **Fase E — stima automatica in base al nucleo familiare**: nel wizard di configurazione iniziale, stima del numero di persone in famiglia usata per proporre budget/distribuzione realistici (supermercato, bollette, ecc.) — versione gratuita solo regole/statistiche (mai AI, coerente con "Free non fa mai chiamate AI"); valutare se arricchirla in Premium con l'AI già esistente (`suggestIncomeDistribution`).
- **Fase F — scanner in famiglia (Premium)**: portare la scansione scontrino/prodotti (oggi solo nelle spese personali) anche nel flusso di spesa familiare, solo Premium/Trial.
- ~~**Fase G — schermata "Novità e aggiornamenti"**~~ — **fatto** (Block 12). Contenuto letto da Firestore (scelta confermata), non scritto a mano nel codice.
- **Fase H — schermata di controllo/analytics riservata all'utente (ultima, la più delicata)**: numero di persone che usano l'app, split Free/Trial/Premium, statistiche di guadagno. Prima di iniziare, proporre come limitare l'accesso solo all'account dell'utente (es. controllo su uid/email specifico) e chiarire che i dati di guadagno reali saranno disponibili solo quando Play Billing sarà davvero attivo (bloccato sui prerequisiti Play Console già noti in Fase L, non dipende da questo lavoro).
- Richieste dell'audit già risultate fatte prima ancora di iniziare queste fasi (nessun lavoro necessario): famiglia demo con nomi+ruoli, "Buffer"→"Fondo imprevisti", spese senza categoria in "Spese generali", creazione busta al volo da una spesa, budget busta opzionale, fix invito famiglia, notifiche push (codice completo, deploy volutamente in pausa — vedi Block 5).

**Block 9 — Fase D: spese fisse ricorrenti (completata e verificata interattivamente).**
- **Modello**: `lib/models/recurring_expense.dart` (`RecurringExpense`: descrizione, importo, `envelopeId`, `dayOfMonth` 1-28 — evita mesi senza il giorno 29/30/31 —, `active`, `lastGeneratedMonthKey` per sapere se il mese corrente è già stato confermato). Nuova subcollection personale `users/{uid}/recurringExpenses`, stesse regole di lettura/scrittura già in uso per buste/spese/entrate/challenge (`firestore.rules`).
- **Servizio**: `FirestoreService` aggiunge `streamRecurringExpenses`/`addRecurringExpense`/`updateRecurringExpense`/`deleteRecurringExpense` (CRUD semplice) e `confirmRecurringExpense` — **l'unico punto in cui una spesa ricorrente diventa una spesa reale**: riusa `addExpense` (stessa scrittura atomica spesa+decremento saldo busta già in uso ovunque) e poi segna il mese corrente come confermato sulla spesa ricorrente. Chiamato **solo** da un tap esplicito dell'utente — nessun trigger automatico, nessuna Cloud Function schedulata: coerente con l'istruzione esplicita del prompt originale ("sempre con conferma esplicita dell'utente, mai automatico/silenzioso").
- **UI**: nuova card "Spese ricorrenti da confermare" in Home (`_recurringExpensesDueCard`, sotto gli avvisi budget, stesso stile neutro `Color(0xFFF8FAFC)` — non è una proposta AI, è la conferma di qualcosa che l'utente ha già definito lui stesso, quindi non segue la convenzione del colore `primary.withOpacity(0.08)` riservata alle proposte AI), visibile solo quando c'è almeno una spesa ricorrente dovuta questo mese e non ancora confermata (`RecurringExpense.isDueOn`, calcolo puro Dart, zero costo, disponibile anche su Free). Nuova schermata `lib/screens/recurring_expenses_screen.dart` (elenco, stesso stile card di `buste_screen.dart`, FAB "+", modifica/elimina) e `lib/screens/new_recurring_expense_screen.dart` (form crea/modifica, riusa il pattern `envelope`-opzionale già visto in `NewEnvelopeScreen`). Raggiungibile da una nuova voce "Spese ricorrenti" nel drawer della Home (icona `HeroIcons.arrowPath`, la stessa già mappata su `CategoryType.abbonamenti` nel design system, per coerenza visiva "ricorrenza").
- **Scelta di scope**: la busta "📦 Spese generali" non è selezionabile per una spesa ricorrente (a differenza di una spesa normale) — una spesa fissa ricorrente ha senso solo legata a una busta reale e tracciabile, non al contenitore generico.
- **Verificato**: `flutter analyze` pulito (32 issue, solo +1 rispetto alle 31 preesistenti per lo stesso pattern `withOpacity` deprecato già usato ovunque nel progetto — nessun nuovo tipo di warning), `flutter build web --release` e `flutter test` ok. **Verifica interattiva reale** end-to-end (emulatori Firestore/Auth/Functions locali, `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64` — nota: serve anche anteporlo al `PATH`, non basta l'env var, altrimenti `firebase emulators:start` continua a trovare il Java 17 di sistema; utente di test reale seminato via Admin SDK con una busta "Casa" (budget €1000) e una spesa ricorrente "Mutuo" €700 al giorno 1, Playwright isolato nello scratchpad, stesso approccio delle sessioni precedenti): login reale → card "Spese ricorrenti da confermare" visibile in Home con "Mutuo · €700.00" e bottone "Registra" → tap → **spesa reale registrata, saldo busta sceso correttamente da €1000 a €300 (70% usato), card scomparsa perché il mese risulta già confermato**, il livello/XP e la card avvisi budget si sono aggiornati di conseguenza → drawer con voce "Spese ricorrenti" funzionante → schermata elenco con la card "Mutuo" (icona, "Il giorno 1 · 🏠 Casa", €700.00, matita/cestino) → form di modifica pre-compilato correttamente con tutti i valori reali. Nessun overflow, nessun errore console. Non testato: cancellazione (stesso identico pattern conferma-poi-elimina già verificato altrove nel progetto, es. buste) e il caso limite di un giorno del mese già passato al momento della creazione (comportamento verificato a livello di codice: risulterebbe subito "dovuta", corretto).

**Block 10 — Fase E: stima automatica in base al nucleo familiare (completata e verificata interattivamente).**
- **Nuovo passaggio nel wizard reale**: `lib/screens/onboarding/setup/setup_household_step.dart` (`SetupHouseholdStep`), tra "Quanto entra ogni mese?" e "Come vuoi organizzare i tuoi soldi?" — stepper 1-8 persone (default 1), nessuna scrittura Firestore propria: è solo un numero passato in memoria al passaggio successivo (`SetupFlow._householdSize`, `lib/screens/onboarding/setup/setup_flow.dart`). Nessun nuovo campo utente su Firestore, coerente con "solo regole/statistiche, mai una scelta tecnica introdotta senza bisogno reale".
- **Applicazione**: `setup_envelopes_step.dart` — ogni `_SuggestedEnvelope` ha ora anche un `householdFactorPerExtraPerson` dichiarato esplicitamente (non una vera scala di equivalenza OECD, che richiederebbe distinguere adulti/minori, dato non raccolto qui: un'approssimazione semplice e trasparente). Budget suggerito = `entrata × quota categoria × (1 + fattore × persone_oltre_la_prima)`. Categorie scalate: **Spesa** (supermercato, fattore 0.5 — quasi lineare con le bocche da sfamare), **Famiglia** (0.3), **Casa** (0.15, bollette che aumentano ma non raddoppiano con più persone). Non scalate (fattore 0, restano legate solo all'entrata): Auto (la rata/assicurazione non cambia con gli occupanti), Svago, Risparmio, Fondo imprevisti (obiettivi scelti dall'utente, non un consumo pro-capite). Il sottotitolo del passaggio buste indica esplicitamente "importi già adattati per N persone in casa" quando N>1, così l'utente capisce da dove arrivano i numeri prima di doverli modificare.
- **Verificato**: `flutter analyze` pulito (32 issue, invariato), `flutter build web --release` e `flutter test` ok. **Verifica interattiva reale end-to-end** (stesso setup emulatori/Playwright dei blocchi precedenti): registrazione di un account nuovo vero → wizard nome → entrata €2000 → nuovo passaggio "In quante persone vivete insieme?" (stepper mostrato correttamente, "1 persona"/"persone" al plurale) → portato a 4 persone col tap su "+" → passaggio buste con sottotitolo "...già adattati per 4 persone in casa" e importi **verificati numericamente uno per uno contro la formula**: Casa €846, Spesa €729, Auto €208, Famiglia €475, Svago €167, Risparmio €417, Fondo imprevisti €83 — tutti esatti. Confermato poi via lettura diretta di Firestore (Admin SDK sull'emulatore) che le 7 buste sono state scritte davvero con questi identici budget dopo "Crea le mie buste", non solo mostrate a schermo. Nessun errore console legato al codice (solo `ERR_INSUFFICIENT_RESOURCES` su richieste asset non legate a Firestore, preesistente e non bloccante). Non verificato visivamente il passaggio finale del wizard (obiettivo/transform) in questa sessione — fuori scope della Fase E, schermate preesistenti non toccate.

**Block 11 — Fase F: scanner in famiglia (completata e verificata interattivamente).** Porta lo scanner scontrino/prodotti (finora solo nelle spese personali) anche nel flusso di spesa familiare, solo Premium/Trial, come da Piano fasi D-H più sotto.
- **Nuova schermata**: `lib/screens/scan_family_receipt_screen.dart` (`ScanFamilyReceiptScreen`, riceve `familyId`), riusa la **stessa** Cloud Function `scanReceipt` già usata dallo scanner personale (`scan_receipt_screen.dart`) — nessuna nuova function, nessun nuovo controllo server: il gate Premium/Trial è quello già esistente dentro `scanReceipt` (`requireActiveAccess`), verificato sull'utente personale che chiama, non sulla famiglia — stesso pattern già motivato per l'analisi AI famiglia (Blocco 8 del piano AI Premium). Stessa `TrialQuotaBadge` (quota condivisa `scontriniUsati`) dello scanner personale.
- **Differenze rispetto allo scanner personale**, per lo stesso motivo già documentato nel Block 2 (buste familiari fuori scope per "Spese generali"): nessuna busta "📦 Spese generali" selezionabile — solo buste familiari reali o "+ Nuova busta" (crea al volo una busta familiare, dialog inline identico a quello personale). Il tipo di spesa familiare (`FamilyExpenseType`: condivisa/personale — se personale, dropdown "Di chi è la spesa?" coi membri reali) si sceglie **una sola volta per l'intero scontrino**, non prodotto per prodotto: un'unica spesa al supermercato ha un solo "chi paga". La modalità "ripartita" non è supportata dallo scanner (richiederebbe percentuali per ogni singola riga prodotto, sproporzionato) — resta disponibile solo nel form manuale (`new_family_expense_screen.dart`). Nessuna card "confronto con lo storico" (a differenza dello scanner personale): `FamilyExpense` non ha un campo `category` proprio (la categoria è ereditata dalla busta, stessa scelta di design già motivata in FASE 3/6), quindi il confronto per categoria non è replicabile senza introdurre un campo dati incoerente col resto del modello familiare.
- **`family_service.dart`**: `addFamilyEnvelope` ora ritorna `Future<String>` (l'id del nuovo documento) invece di `Future<void>` — necessario per il "+ Nuova busta" inline dentro lo scanner (deve sapere subito quale busta appena creata selezionare, stesso bisogno già risolto per `FirestoreService.addEnvelope` personale).
- **Bottone d'ingresso**: `family_screen.dart`, "Scansiona scontrino famiglia" sotto "Nuova spesa familiare", gated Premium/Trial con lo stesso pattern già in uso in `home_screen.dart` (`_goPremiumGated`): se l'utente personale ha `hasAiAccess` naviga alla nuova schermata, altrimenti alla schermata Premium — mai un bottone semplicemente nascosto, coerente con la scelta già fatta per le altre azioni Premium in Home (un bottone disabilitato/nascosto non spiegherebbe perché, un redirect a Premium sì).
- **Verificato**: `flutter analyze` pulito (39 issue, +7 rispetto alle 32 preesistenti, tutti dello stesso tipo `withOpacity`/`RadioListTile` deprecato già usato ovunque nel progetto — nessun nuovo tipo di warning), `flutter build web --release` ok. **Verifica interattiva reale** (emulatori Firestore/Auth/Functions/Pub-Sub locali, `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64`, Playwright, viewport 390×844, due utenti reali seminati via Admin SDK: "Alessandro" Premium con famiglia+busta+membro, "Paolo" Free nella stessa famiglia): login Premium → Famiglia → bottone visibile → tap → naviga davvero a "Scanner scontrino famiglia" (non a Premium); selettore modalità foto funzionante, card "Chi paga questa spesa?" con le due opzioni, selezionando "Personale" il dropdown si popola coi membri reali, bottone "Scansiona" correttamente disabilitato senza foto, nessun overflow in nessuno stato; login Free → Famiglia → tap sullo stesso bottone → reindirizzato correttamente a Premium, non allo scanner. **Deliberatamente non testato**: la scansione vera (pressione di "Scansiona" con foto reali) — `functions/.env` contiene la vera `GEMINI_API_KEY` di produzione, caricata anche dall'emulatore Functions, quindi una scansione reale avrebbe generato una vera chiamata Gemini a pagamento; non eseguita senza chiedere, stesso principio già applicato in FASE 8/Block 5 per altri test di gate AI. Di conseguenza anche il dialog "+ Nuova busta" e la lista prodotti/dropdown busta (raggiungibili solo dopo una scansione riuscita) non sono stati verificati interattivamente in questa sessione, solo tramite lettura del codice (riusano esattamente lo stesso pattern già verificato per lo scanner personale).
- **Nota minore trovata durante la verifica, non una regressione di questo blocco**: un assert di debug Flutter ("ListTile background color or ink splashes may be invisible") compare sulla card "Chi paga questa spesa?" a causa del `Container` con `color` che avvolge i `RadioListTile` — lo stesso identico pattern esiste già, invariato, in `new_family_expense_screen.dart` (schermata preesistente, mai segnalato prima). Solo un assert di debug, nessun impatto visivo reale per l'utente finale — segnalato qui come miglioria futura (avvolgere i `RadioListTile` in un `Material` proprio), non corretto in questo blocco per restare nello scope della Fase F.

**Block 12 — Fase G: schermata "Novità e aggiornamenti" (completata e verificata interattivamente).** Come da domanda posta nel piano fasi D-H, la scelta tra contenuto scritto a mano nel codice o letto da Firestore era già stata decisa (contenuto letto da Firestore, aggiornabile senza una nuova build dell'app) in una sessione precedente, interrotta a metà subito dopo aver scritto il codice ma prima di verificarlo — ripresa in questa sessione partendo dal lavoro già presente, senza ridiscutere la scelta.
- **Modello**: `lib/models/changelog_entry.dart` (`ChangelogEntry`: `version`, `date`, `title`, `items`), letto da una nuova collection Firestore **top-level** `changelog/{entryId}` (non sotto `users/{uid}`: è contenuto pubblico uguale per tutti gli utenti, non dati personali). Il campo `date` è una stringa ISO (`"2026-09-02"`), scritta a mano insieme al resto della voce — nessun campo `Timestamp` da gestire lato client.
- **Regole**: `firestore.rules`, `match /changelog/{entryId} { allow read: if request.auth != null; allow write: if false; }` — qualunque utente autenticato legge, il client non scrive mai (una voce di changelog falsa scritta da un utente sarebbe indistinguibile da una reale se il client potesse scriverla). Il contenuto si aggiunge solo a mano (Firebase Console o Admin SDK) ad ogni release.
- **Servizio**: `FirestoreService.streamChangelog()` (`firestore_service.dart`) — `orderBy('date', descending: true)`, così la voce più recente compare per prima senza bisogno di un campo `order` separato.
- **UI**: nuova `lib/screens/whats_new_screen.dart` (`WhatsNewScreen`), AppBar bianca/elevation 0/ink coerente col resto dell'app, una card per voce (sfondo neutro `Color(0xFFF8FAFC)`, `borderRadius` 16 — è contenuto informativo, non una proposta AI, quindi non segue la convenzione del colore `primary.withOpacity(0.08)`) con icona megafono, titolo, "vVERSIONE · data in italiano" e l'elenco puntato delle novità. Le date sono formattate in italiano con una tabella statica dei nomi dei mesi (`_formatDateIt`), non con `intl`/`DateFormat`, perché `initializeDateFormatting` non è mai stato inizializzato in questo progetto (avrebbe richiesto un nuovo setup solo per questa schermata). Raggiungibile da una nuova voce "Novità e aggiornamenti" nel drawer della Home (`home_screen.dart`, tra "Spese ricorrenti" ed "Esci", icona `HeroIcons.megaphone`).
- **Verificato**: `flutter analyze` pulito (40 issue, +1 rispetto alle 39 preesistenti, stesso pattern `withOpacity` già usato ovunque — nessun nuovo tipo di warning), `flutter build web --release` ok. **Verifica interattiva reale end-to-end** (emulatori Firestore/Auth locali, `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64`; ambiente sotto forte pressione di memoria in questa sessione — swap quasi pieno per via di VS Code e degli altri strumenti già in esecuzione, causa di un primo tentativo fallito con `ERR_INSUFFICIENT_RESOURCES` nel caricamento di CanvasKit — risolto fermando il dev-server Flutter con hot-reload, molto più pesante in RAM di una build statica, e servendo invece una `flutter build web --debug` con un semplice server HTTP Python): utente di test reale seminato via Admin SDK con due voci di changelog vere, login reale (Playwright, viewport 390×844) → drawer → tap su "Novità e aggiornamenti" → schermata con le due card nell'ordine corretto (più recente prima), date in italiano corrette ("2 settembre 2026", "15 agosto 2026"), nessun overflow, nessun errore in console. Confermato anche via chiamata REST diretta all'emulatore Firestore che un tentativo di scrittura non autenticata sulla collection `changelog` viene rifiutato (`PERMISSION_DENIED`), a conferma che `allow write: if false` funziona davvero e non solo sulla carta.
- **Nota per sessioni future**: se l'ambiente risulta di nuovo sotto pressione di memoria durante una verifica interattiva (swap quasi pieno, `ERR_INSUFFICIENT_RESOURCES` nel browser), preferire `flutter build web --debug` servito con un HTTP server statico invece di `flutter run -d web-server` — il dev-server con hot-reload tiene in vita un processo `frontend_server_aot`/DDC che da solo occupa oltre 1 GB di RAM, non necessario per una verifica una tantum che non richiede hot-reload.

## Problemi noti
- **APK release: schermata bianca che sparisce subito all'avvio, da verificare dopo il fix.** Diagnosticato da due log logcat reali forniti dall'utente (`log_smart_budget.txt`, `crash_log.txt`, telefono Xiaomi/MIUI): crash riproducibile 3 volte di fila, sempre identico — `FATAL EXCEPTION: main`, `Unable to get provider androidx.startup.InitializationProvider: ... Failed to create an instance of androidx.work.impl.WorkDatabase`. Il crash avviene durante l'inizializzazione dei `ContentProvider` di Android, prima ancora di `MainActivity`/Flutter — coerente con "schermata bianca che sparisce subito", non un bug nel codice Dart. **Causa**: `androidx.work.impl.WorkDatabase` (Room, inizializzato automaticamente da `firebase_messaging`, l'unica dipendenza che usa WorkManager nel progetto — aggiunta nel Block 5 notifiche push) non riusciva a essere istanziato in build release; i nomi delle classi nello stack trace sono tutti offuscati (`r8-map-id-...`), a conferma che la build era passata per la minificazione R8, ma il progetto **non ha mai avuto un `proguard-rules.pro`** — nessuna regola proteggeva le classi Room/WorkManager generate e accedute via reflection, che R8 rimuoveva/rinominava. **Fix**: `android/app/build.gradle.kts` ora disattiva esplicitamente `isMinifyEnabled`/`isShrinkResources` per il build type release (prima erano impliciti/non dichiarati, evidentemente non risultavano `false` nella build testata dall'utente); aggiunto `android/app/proguard-rules.pro` con le regole keep per Room/WorkManager, già collegato in `proguardFiles` così sarà pronto quando in futuro si vorrà riattivare la minificazione (richiederà però una nuova verifica reale, non eseguibile qui). **Rischio**: nessuno per il funzionamento dell'app, solo un APK più grande finché la minificazione resta disattivata — accettabile per una fase di beta test, da rivedere prima della pubblicazione reale (comunque già bloccata su altri prerequisiti, vedi Fase L). **Test**: non verificabile in questo ambiente (nessun Android SDK installato, confermato più volte in questo file) — **da ricompilare e testare sul dispositivo reale dall'utente**, unico modo per confermare che il fix risolve davvero il crash.
- ~~`test/widget_test.dart` fallisce sempre~~ — **verificato in FASE 11: non riproducibile, nota superata**. `flutter test` passa pulito, eseguito due volte per escludere flakiness. Il test attuale (`App builds without throwing`) fa il `pumpWidget` di `SmartBudgetApp` senza mai attraversare un punto che richieda `Firebase.initializeApp()` durante il primo frame (`_RootGate` mostra uno spinner mentre l'onboarding flag si carica in modo asincrono), quindi il problema descritto qui non si presenta con la versione attuale del file. Lasciato com'è, nessun intervento necessario.
- ~~`chatWithAssistant` inventa conferme false per richieste di azione~~ — **corretto**. Il prompt in `functions/index.js` ora contiene una "REGOLA FONDAMENTALE" esplicita: niente capacità di scrittura, mai fingere di aver eseguito un'azione, e in caso di richiesta di azione spiegare dove/come farla da soli nell'app. Riverificato con lo stesso identico test che aveva scoperto il bug ("Crea una busta...", "Registra una spesa...") — ora risponde correttamente rifiutando e indirizzando, senza inventare conferme; verificata anche l'assenza di regressioni su una domanda informativa legittima.
- ~~Onboarding al primo avvio~~ — **implementato, poi sostituito**. Prima versione: `lib/screens/onboarding_screen.dart` (4 pagine statiche) + `lib/services/onboarding_service.dart` (flag `shared_preferences`, per dispositivo non per account), con `_RootGate` in `main.dart` a decidere tra onboarding, login e `BottomNavShell`. Sostituita dalla demo interattiva a 11 pagine + wizard di configurazione reale — vedi "Onboarding avanzato" più sopra (`onboarding_service.dart` e il flag riusati identici, solo il contenuto delle pagine è cambiato).
- ~~Il saluto in Home mostrava sempre "Ciao Alessandro!" per qualunque utente~~ — **corretto**. Il nome era hardcoded in `home_screen.dart`. Ora usa `FirebaseAuth.currentUser?.displayName` (fallback "Ciao!" se non impostato, mai un nome finto): campo "Nome" aggiunto al form di registrazione (`login_screen.dart`, chiama `updateDisplayName` dopo la creazione account) e un'icona di modifica nel drawer della Home per gli account già esistenti senza nome impostato.

## Note economiche/di prodotto da rispettare nelle scelte tecniche
- Modello Gemini: `gemini-3.5-flash-lite` per le chiamate correnti (verificato disponibile)
- Costo AI target: sotto €0,15/utente Premium/mese — instradare verso il modello più economico possibile per ogni tipo di richiesta
- Non salvare le foto scontrino originali dopo l'elaborazione, salvo scelta esplicita dell'utente (risparmio storage)
